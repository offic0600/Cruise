version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: a17114b58ff361355848fba72165989333699328
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-30
  title: 评估 legacy `/issues/[id]` route 无 slug 空 back-link 断言是否可与失败 helper 进一步收口
  status: done
  summary: >-
    已确认 legacy `/issues/[id]` route 当前“无 workspace slug”route 级空 back-link 断言与 IMP-29 新增的失败分支共享 helper 表达的是同一降级 contract；本轮直接让 route 无 slug 场景复用 `expectLegacyIssueRouteEmptyBackLinkContract()`，保留 helper 级 `buildIssueDetailRouteBackLinkProps(...)` 无 slug 空对象断言不变，从而在不扩大实现面的前提下继续收口 legacy route 空 back-link 期望。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: a17114b58ff361355848fba72165989333699328
  next_task: IMP-31
current_task:
  id: IMP-31
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 空 back-link helper 是否可继续收口 route/helper 命名与调用边界
  lane: Implementation
  status: pending
  done_when: >-
    在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 当前 `expectLegacyIssueRouteEmptyBackLinkContract()` 与 helper 级无 slug 空对象断言的命名/调用边界是否仍存在可安全收紧的重复；若可行，则继续只做一刀测试收口并保持定向验证通过。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-31 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
