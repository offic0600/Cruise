version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 28893be2fe2143b01ed57f8f94f19c89e3a1090b
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-24
  title: 为 `/issues/[id]` 直达 issue 详情页补最小显式 back-link contract
  status: done
  summary: >-
    `/issues/[id]` 直达页现已在服务端先获取 issue 与 organizations，按 `organizationId` 解析 workspace slug 后显式构造并传入 `IssueDetailPage` 的 `href/label` back-link contract，从而消除顶部返回链接对组件内部默认 fallback 的依赖；`IssueDetailPage` 仍仅保留删除后跳转所需的内部 `teamActivePath(...)` 导航兜底，不再承担页面顶部 CTA 的缺省生成职责。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 51 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 28893be2fe2143b01ed57f8f94f19c89e3a1090b
  next_task: IMP-25
current_task:
  id: IMP-25
  parent_task: linear-parity-push
  title: 为 `/issues/[id]` 直达页补最小 route seam 回归测试以锁定显式 back-link contract
  lane: Implementation
  status: pending
  done_when: >-
    为 `/issues/[id]` 直达 issue 详情页新增最小 route seam 回归测试，锁定其向 `IssueDetailPage` 显式透传 `href/label` back-link contract，并覆盖无可解析 workspace slug 时的安全降级行为。
  blocker: >-
    暂无外部 blocker；当前代码路径已显式传入 back-link contract，但仍缺少针对 legacy `/issues/[id]` 直达页的独立 seam 测试，下一轮应优先补齐该验证而不是继续扩大导航重构范围。
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-22 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
