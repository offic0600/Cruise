version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: PLACEHOLDER_FEATURE_SHA
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-47
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route `getOrganizations()` 失败场景测试标题是否也应按 route render 语义从 falls back 收口为 renders
  lane: Implementation
  status: done
  summary: >-
    已复核 legacy `/issues/[id]` route 中 `getOrganizations()` 失败场景的断言层级：该测试与上一轮的
    `getIssue(...)` 失败场景一样，都是 route 级渲染 legacy `/issues/[id]` route 后，再校验 `IssueDetailPage`
    收到共享空 props contract，而不是 helper 返回值 contract；因此本轮将第二条安全标题从
    `falls back to the shared empty props contract when getOrganizations rejects in the legacy /issues/[id] route`
    收口为 `renders the shared empty props contract when getOrganizations rejects in the legacy /issues/[id] route`，
    用 `renders` 与同组 route 级 API 失败场景保持一致，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: PLACEHOLDER_FEATURE_SHA
  next_task: IMP-48
current_task:
  id: IMP-48
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均改为 renders 后是否仍需保留 shared wording
  lane: Implementation
  status: pending
  done_when: >-
    在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 legacy `/issues/[id]` route API
    失败场景当前均为 `renders the shared empty props contract ...` 后，标题中的 `shared` wording 是否仍有必要；若可行，
    则只改第一处安全标题或明确保留理由，并保持定向验证通过。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-47 的一个最小可交付增量；完成或阻塞后必须写回状态并停止.
