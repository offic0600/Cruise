version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 3f4ee44e83ae75ab203922ad334920bd421c2e40
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
  docs_head: 090dfe370a56286fbb5a29ab481eea0eaeeb153c
last_completed_task:
  id: IMP-207
  parent_task: linear-parity-push
  title: 重新评估在当前再次双无 route wording 组合下第一条 API 失败标题是否需要恢复 route 级语义词
  lane: Implementation
  status: done
  summary: >-
    已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses`
    明确保留 helper seam 语义；在当前 `getIssue rejects` / `getOrganizations rejects` 双无 route wording 组合下，
    第一条 API 失败标题若继续维持无 route 级语义词，会与 helper 级 lookup 标题层级过于接近。
    因此本轮仅将第一条标题恢复为 `route getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture
    设置与 helper/route 运行时逻辑不变。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && ./node_modules/.bin/tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 3f4ee44e83ae75ab203922ad334920bd421c2e40
  next_task: IMP-208：重新评估在当前再次非对称 wording 组合下第二条 API 失败标题是否需要恢复 route 级语义词
current_task:
  id: IMP-208
  parent_task: linear-parity-push
  title: 重新评估在当前再次非对称 wording 组合下第二条 API 失败标题是否需要恢复 route 级语义词
  lane: Implementation
  status: pending
  done_when: >-
    基于当前 `route getIssue rejects` / `getOrganizations rejects` 组合，复核第二条 API 失败场景标题是否需要恢复为
    `route getOrganizations rejects` 以继续维持与 helper 级 lookup 标题的层级区分；若需要，则仅改这一处标题并保持共享断言、fixture 设置与 helper/route
    运行时逻辑不变，随后通过 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、`cd frontend && ./node_modules/.bin/tsc --noEmit`
    与 `git diff --check` 验证。
  blocker: >-
    当前 state 与 task-board 已对齐到 IMP-207 待收口完成，下一刀可直接继续 implementation 微调；无额外 blocker.
