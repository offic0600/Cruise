version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: fc39ffb39f2c7311e7cd04db35845c2758c41171
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
  docs_head: 3f02f3754a9f1920bff741bbe384f9c0c42918d5
last_completed_task:
  id: IMP-167
  parent_task: linear-parity-push
  title: 重新评估第二条 route 级 API 失败标题是否需要恢复 route wording，以维持 helper/route 层级区分
  lane: Implementation
  status: done
  summary: >-
    已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses`
    明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持
    `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup
    标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条
    `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && ./node_modules/.bin/tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: fc39ffb39f2c7311e7cd04db35845c2758c41171
  next_task: IMP-168：重新评估第一条 API 失败标题是否需要恢复 route wording，以维持 helper/route 层级区分
current_task:
  id: IMP-168
  parent_task: linear-parity-push
  title: 重新评估第一条 API 失败标题是否需要恢复 route wording，以维持 helper/route 层级区分
  lane: Implementation
  status: pending
  done_when: >-
    基于当前 `getIssue rejects` / `route getOrganizations rejects` 组合，复核第一条 API 失败场景标题是否仍需恢复为
    `route getIssue rejects` 以维持与 helper 级 lookup 标题的层级区分；若需要，则仅改这一处标题并保持共享断言、
    fixture 设置与 helper/route 运行时逻辑不变，随后通过 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、
    `cd frontend && ./node_modules/.bin/tsc --noEmit` 与 `git diff --check` 验证。
  blocker: >-
    当前 state 与 task-board 已对齐到 IMP-167 收口完成，下一刀可直接继续 implementation 微调；无额外 blocker。
