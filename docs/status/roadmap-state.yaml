     1|version: 1
     2|roadmap:
     3|  path: docs/plans/2026-04-16-linear-parity-roadmap.md
     4|  total_tasks: 2
     5|project:
     6|  repo_path: /Users/liuzheng/Desktop/Cruise
     7|  branch: codex/unify-issue-model
     8|  head: 7d68c649f00463d86786f66aa5211ea84bb4ea75
     9|  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
    10|last_completed_task:
    11|  id: IMP-51
    12|  parent_task: linear-parity-push
    13|  title: 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 contract wording
    14|  lane: Implementation
    15|  status: done
    16|  summary: >-
    17|    已复核 legacy `/issues/[id]` route 中 `getOrganizations()` API 失败场景当前同样属于 route 级渲染后断言
    18|    `IssueDetailPage` 收到空 props contract 的观察面；由于共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()`
    19|    已在命名中明确表达该 contract 为共享入口，因此测试标题中的 `shared` wording 与 helper 语义重复。
    20|    本轮仅将该场景标题从 `renders the shared empty props contract when getOrganizations rejects in the legacy /issues/[id] route`
    21|    收口为 `renders the empty props contract when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、
    22|    fixture 设置与 helper/route 运行时逻辑不变。
    23|  tests:
    24|    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    25|    - cd frontend && npx tsc --noEmit: passed
    26|    - git diff --check: passed
    27|  review:
    28|    code: approved
    29|  commit: 7d68c649f00463d86786f66aa5211ea84bb4ea75
    30|  next_task: IMP-51
    31|current_task:
    32|  id: IMP-50
    33|  parent_task: linear-parity-push
    34|  title: 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 shared wording 后是否仍需保留 empty props contract 全称
    35|  lane: Implementation
    36|  status: pending
    37|  done_when: >-
    38|    在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 API 失败场景当前均为
    39|    `renders the empty props contract ...` 后，标题中的 `empty props contract` 全称是否仍存在可安全收口的一处；若可行，
    40|    则只改第一处安全标题并保持定向验证通过。
    41|  blocker: '-'
    42|notes:
    43|  execution_rules:
    44|    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    45|    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    46|    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    47|    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    48|    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
    49|  next_run_prompt_summary: >-
    50|    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-51 的一个最小可交付增量；完成或阻塞后必须写回状态并停止.
    51|