version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 5f9bbf40737ed971c5a7c4d446c68345454a161f
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-19
  title: 让 issue detail route 真正消费共享 shell model 渲染 loading/empty-state 分支
  status: done
  summary: >-
    issue detail route 的 loading/background-refetch/error/no-id 分支现已真正消费 `buildIssueDetailRouteShellModel(...)` 产出的共享 shell model，删除 route 内部重复 empty-state 组装，并保持 scope 仅限 route 分支 seam。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 50 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: PENDING_FEATURE_COMMIT
  next_task: IMP-20
current_task:
  id: IMP-20
  parent_task: linear-parity-push
  title: 继续收口 issue detail route 与 IssueDetailPage 之间剩余 skeleton/empty-state seam
  lane: Implementation
  status: pending
  done_when: >-
    在不触碰 capture lane 的前提下，继续寻找 issue detail route 与 `IssueDetailPage` 之间仍重复的 skeleton/empty-state 或 back-link seam，并完成一个最小可验证收口增量的实现、验证、提交、状态写回与推送闭环。
  blocker: >-
    暂无新增外部 blocker；下一轮应先基于当前 route shell 已消费的前提，选择一处仍然重复的 page/route seam 做单点收口，避免扩大到整页重构。

  - id: CAP-08
    lane: Capture
    status: blocked
    summary: >-
      因当前工作树混有大量 implementation 遗留脏变更，capture lane 本轮未继续落盘新证据，而是将 CAP-08 真实写回为 blocked，并建议在独立干净工作树或先完成 implementation 收口后继续同一 Add label 入口的反馈态只读采集。
    tests:
      - last-status consumer_policy check: passed
      - scope judgement on mixed dirty tree: passed
    review:
      evidence: approved
    commit: none
    next_task: CAP-08

    status: done
    summary: >-
      已完成 issue 详情页默认态截图、可见模块与 network-idle 摘要采集，形成 CAP-07 基线证据包。
    tests:
      - last-status consumer_policy check: passed
      - issue detail page read-only capture: passed
    review:
      evidence: approved
    commit: none
    next_task: CAP-08
  - id: IMP-16
    lane: Implementation
    status: done
    summary: >-
      已为 team-active 工具栏状态说明补 helper 级回归测试，并定向验证通过。
    tests:
      - frontend vitest targeted helper tests: passed
    review:
      code: approved
    commit: historical
    next_task: IMP-17
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-17 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
