version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 55fb9752f5ee5c27e0fb433543b0c7933e3df11a
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-27
  title: 评估 legacy `/issues/[id]` route 是否可抽出共享 issue/org lookup seam 以消除重复 Promise 组装
  status: done
  summary: >-
    已将 legacy `/issues/[id]` route 内部重复的 `Promise.all(getIssue, getOrganizations)` 与 workspace slug→back-link 组装收口为 `loadIssueDetailRouteLookup(...)` 和 `buildIssueDetailRouteBackLinkProps(...)` 两个共享 seam，并补 routes seam 回归测试锁定成功解析与无 slug 时的空 contract。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 55fb9752f5ee5c27e0fb433543b0c7933e3df11a
  next_task: IMP-28
current_task:
  id: IMP-28
  parent_task: linear-parity-push
  title: 为 legacy `/issues/[id]` lookup seam 补最小 route 级失败/成功 contract 断言去除 route 与 helper 断言重复
  lane: Implementation
  status: pending
  done_when: >-
    在不扩大实现面的前提下，复核 `loadIssueDetailRouteLookup(...)` / `buildIssueDetailRouteBackLinkProps(...)` 与 legacy route 现有测试边界，若存在 route 级与 helper 级重复断言，则收紧为一组更稳定的最小 contract 断言并保持定向验证通过。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-28 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
