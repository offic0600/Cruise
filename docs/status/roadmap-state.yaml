version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 2f1eeb8f47fb02c4a8db0f211ab42b5524daaf52
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-33
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 空 back-link 测试文案是否还可继续统一为 shared legacy lookup seam 语义
  lane: Implementation
  status: done
  summary: >-
    已将 workspace slug miss 场景的 route 级测试标题改为明确表达“legacy route 透传 shared legacy lookup seam 的空 back-link contract”，把 route、shared lookup seam 与 miss path 的语义统一到同一句文案中，而不扩大到实现代码或其他测试结构。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 2f1eeb8f47fb02c4a8db0f211ab42b5524daaf52
  next_task: IMP-34
current_task:
  id: IMP-34
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 空 back-link helper/contract 测试命名是否还可继续去掉重复 empty back-link 短语
  lane: Implementation
  status: pending
  done_when: >-
    在不扩大实现面的前提下，复核 `expectLegacyIssueDetailRouteEmptyBackLinkProps()` 与 `expectLegacyIssueRouteEmptyBackLinkContract()` 及其调用测试标题是否仍存在可安全压缩的重复 empty/back-link 词组；若可行，则只做一刀测试命名收口并保持定向验证通过。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-34 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
