version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: e9067b1ec55617bc6deab8e00a221873f05ee0a5
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
  docs_head: e5fc204f96bb5d5cbdfa03e6b099e2974553337a
last_completed_task:
  id: IMP-164
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词
  lane: Implementation
  status: done
  summary: >-
    已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses`
    明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持
    `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup
    标题层级过于接近。因此本轮仅将第二条标题补回为 `route getOrganizations rejects`，并保持第一条
    `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && ./node_modules/.bin/tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: e9067b1ec55617bc6deab8e00a221873f05ee0a5
  next_task: 待从 docs/linear-parity/task-board.md 续选新的 Implementation lane 任务
current_task:
  id: CAP-08
  parent_task: linear-parity-push
  title: 采集 issue 创建/编辑入口的打开态与校验态证据
  lane: Capture
  status: blocked
  done_when: >-
    只读优先，允许打开弹窗/抽屉并记录字段、默认值、校验反馈，不做持久提交；完成后立即把 task-board、roadmap-state、dev-logbook、worktime 写回。
  blocker: >-
    watchdog 仍显示 consumer_policy=attached_cdp_only 且 websocket_attach=ok，本轮不存在附着失败；但 tmp/linear-capture/summary.json
    仍早于 task-board/roadmap-state/dev-logbook/worktime 的最近写回，说明现有 capture 产物已完成 docs 闭环。当前阻塞点是工作树闭环风险：状态文件与分支近期提交仍在持续收口
    implementation lane，若此时继续新增 capture 落盘，无法保证后续按 capture 单 lane 边界安全收口。应优先在独立干净工作树执行 CAP-08 的只读采集，或待 implementation
    收口完成后再继续 Add label search input 的空结果/建议反馈态证据。
