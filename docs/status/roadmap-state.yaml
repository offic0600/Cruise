version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 28acbdcd9645ca1b20a459feac130299f1cfe57c
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-26
  title: 为 legacy `/issues/[id]` route 补 API 失败场景 seam 测试以锁定 try/catch 降级 contract
  status: done
  summary: >-
    已为 legacy `/issues/[id]` 直达 route 新增最小 seam 回归测试，补齐 `getIssue(...)` 与 `getOrganizations()` 任一抛错时的 try/catch 降级 contract，锁定 route 仍以空 back-link props 渲染 `IssueDetailPage`，不向页面层泄漏 API 失败异常。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 55 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 159949a8ad41624b247133735597194c37762a28
  next_task: IMP-27
current_task:
  id: IMP-27
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 是否可抽出共享 issue/org lookup seam 以消除重复 Promise 组装
  lane: Implementation
  status: pending
  done_when: >-
    识别 legacy `/issues/[id]` route 与相关 issue detail routes 是否仍存在可最小收口的 issue/org lookup 重复组装；若存在，则落一个只触及单一 seam 的 helper 或 contract 提炼，并补最小回归测试。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-27 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
