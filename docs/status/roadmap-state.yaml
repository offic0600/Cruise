version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 8b594cb09f1bec44666b5a1370831c2db3adb3b9
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-64
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 route 全称后是否仍需保留 when wording
  lane: Implementation
  status: done
  summary: >-
    已复核 legacy `/issues/[id]` route 两条 API 失败场景当前都属于 route 级渲染后断言
    `IssueDetailPage` 收到空 props 的观察面；在同一 describe 已限定 legacy `/issues/[id]` route
    上下文、且对称第二条标题 `renders when getOrganizations rejects` 仍保留 `when` wording 作为对照的前提下，
    本轮仅将第一条 `getIssue(...)` 失败场景标题从 `renders when getIssue rejects` 收口为
    `renders getIssue rejects`，以验证去掉第一处 `when` 连接词后标题仍保持可辨识，同时保持共享断言、fixture 设置与
    helper/route 运行时逻辑不变。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 8b594cb09f1bec44666b5a1370831c2db3adb3b9
  next_task: IMP-65
current_task:
  id: IMP-65
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 when wording
  lane: Implementation
  status: pending
  done_when: >-
    在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders when getOrganizations rejects`
    标题中的 `when` wording 是否也可像上一轮 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持
    定向验证通过。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件；若当前运行的是 capture cron 且状态文件 lane 仍停在 implementation，则按 task-board 回退到 Capture lane 当前项 CAP-08。先核对 watchdog attached_cdp_only 约束与 tmp/linear-capture 是否存在比 docs 更新的新证据；若没有新产物且 docs 已同步，则保持 CAP-08 blocked 结论并停止；若存在新产物，再只做一刀 capture docs 收口。implementation cron 仍只推进 IMP-65 的一个最小可交付增量；完成或阻塞后必须写回状态并停止.
