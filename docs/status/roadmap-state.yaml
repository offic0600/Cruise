version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 035fafae7748d3f16e601619ed57fca616f0bf16
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-22
  title: 让 IssueDetailPage 直接消费共享 back-link contract props
  status: done
  summary: >-
    `IssueDetailPage` 现已把返回链接 props 收口为与 route shell `pageBackLink` 一致的 `href/label` contract，删除组件层对 `backHref/backLabel` 旧命名的专用接口，只保留组件内部对缺省 workspace/team 返回链接的 fallback 行为。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 51 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 035fafae7748d3f16e601619ed57fca616f0bf16
  next_task: IMP-23
current_task:
  id: IMP-23
  parent_task: linear-parity-push
  title: 评估并收紧 IssueDetailPage 内部 workspace/team 默认 back-link fallback 的保留边界
  lane: Implementation
  status: pending
  done_when: >-
    在不扩大到 capture lane 或整页导航重构的前提下，核对 `IssueDetailPage` 内部 `teamActivePath(...)` fallback 是否仍为唯一必要兜底；若可进一步收紧则完成最小实现与验证，否则把必须保留 fallback 的原因写回状态与 task-board。
  blocker: >-
    暂无新增外部 blocker；下一轮应先确认除 route 外是否还有直接渲染 `IssueDetailPage` 且依赖内部 fallback 的调用点，再决定能否安全移除或收紧该 fallback。
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-22 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
