version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 7863d4dff513306e6b12e0553c64e969d5dbb2bb
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-31
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 空 back-link helper 是否可继续收口 route/helper 命名与调用边界
  lane: Implementation
  status: done
  summary: >-
    已确认 legacy `/issues/[id]` route 当前 helper 级无 slug 空对象断言与 route 级空 props 断言仍共享同一“空 back-link”语义边界；本轮提炼 `expectEmptyLegacyIssueDetailRouteBackLinkProps()` 作为 helper 级唯一断言入口，并让 `expectLegacyIssueRouteEmptyBackLinkContract()` 在 route 级断言前先复用该 helper，从而继续收紧命名/调用边界，同时保持 helper 返回空对象 contract 与 route 渲染空 props contract 的层级分离。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 7863d4dff513306e6b12e0553c64e969d5dbb2bb
  next_task: IMP-32
current_task:
  id: IMP-32
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route 空 back-link helper 命名是否可进一步对齐 legacy/route 语义
  lane: Implementation
  status: pending
  done_when: >-
    在不扩大实现面的前提下，复核 `expectEmptyLegacyIssueDetailRouteBackLinkProps()` 与 `expectLegacyIssueRouteEmptyBackLinkContract()` 的命名是否仍存在 legacy/route/helper 语义不够一致的轻微噪音；若可行，则只做一刀测试命名收口并保持定向验证通过。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-32 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
