version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 468b79ac1fd0e4f93bb3d484e446a401fbad9cfd
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-39
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route route 级 workspace slug miss 场景测试标题是否还可继续去掉 legacy /issues/[id] 重复路径 wording
  lane: Implementation
  status: done
  summary: >-
    已将 route 级 workspace slug miss 场景测试标题从
    “falls back to the shared empty props contract when the legacy /issues/[id] route workspace slug lookup misses”
    收口为 “falls back to the shared empty props contract when the workspace slug lookup misses”，
    去掉重复的 `legacy /issues/[id] route` 路径 wording，同时保持共享
    `expectLegacyIssueRouteEmptyPropsContract()` 断言、helper/route 运行时逻辑与断言结构不变。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 468b79ac1fd0e4f93bb3d484e446a401fbad9cfd
  next_task: IMP-40
current_task:
  id: IMP-40
  parent_task: linear-parity-push
  title: 评估 legacy `/issues/[id]` route helper/route 两个 workspace slug miss 场景测试标题是否还可继续统一 lookup miss wording
  lane: Implementation
  status: pending
  done_when: >-
    在不扩大实现面的前提下，复核 helper 级 “cannot resolve a workspace slug” 与 route 级
    “workspace slug lookup misses” 两个 workspace slug miss 场景测试标题是否仍可安全统一为更一致的
    lookup miss wording；若可行，则只改第一处可安全收口的测试文案并保持定向验证通过。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-40 的一个最小可交付增量；完成或阻塞后必须写回状态并停止.
