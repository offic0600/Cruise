version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 6d39bf8efd1ef9ed23981dbdda908ccbe7b64d65
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
  docs_head: 373e54cb63e32a2ee8b7720e68b4027bc349c5b5
last_completed_task:
  id: IMP-172
  parent_task: linear-parity-push
  title: 重新评估第二条 API 失败标题在当前恢复第一条 route wording 后是否需要恢复 route wording
  lane: Implementation
  status: done
  summary: >-
    已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses`
    明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且当前保持
    `route getIssue rejects` / `getOrganizations rejects` 组合时，第二条 `getOrganizations rejects` 若继续不带 route wording，会与 helper 级 lookup
    标题层级过于接近，不利于继续维持 route/helper 层级区分。因此本轮仅将该标题恢复为 `route getOrganizations rejects`，同时保持第一条
    `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 57 tests)
    - cd frontend && ./node_modules/.bin/tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 6d39bf8efd1ef9ed23981dbdda908ccbe7b64d65
  next_task: IMP-173：重新评估第一条 API 失败标题在当前再次双 route wording 组合下是否仍可再次收口为无 route wording
current_task:
  id: IMP-173
  parent_task: linear-parity-push
  title: 重新评估第一条 API 失败标题在当前再次双 route wording 组合下是否仍可再次收口为无 route wording
  lane: Implementation
  status: pending
  done_when: >-
    基于当前 `route getIssue rejects` / `route getOrganizations rejects` 组合，复核第一条 API 失败场景标题是否仍可安全收口为
    `getIssue rejects` 且继续维持与 helper 级 lookup 标题的层级区分；若可行，则仅改这一处标题并保持共享断言、fixture
    设置与 helper/route 运行时逻辑不变，随后通过 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、
    `cd frontend && ./node_modules/.bin/tsc --noEmit` 与 `git diff --check` 验证。
  blocker: >-
    当前 state 与 task-board 已对齐到 IMP-172 收口完成，下一刀可直接继续 implementation 微调；无额外 blocker。
