version: 1
roadmap:
  path: docs/plans/2026-04-16-linear-parity-roadmap.md
  total_tasks: 2
project:
  repo_path: /Users/liuzheng/Desktop/Cruise
  branch: codex/unify-issue-model
  head: 9dd1828bf6c5d3c3fe1c5ce7f67d9233291a73fa
  baseline_commit: 5f9bbf40737ed971c5a7c4d446c68345454a161f
last_completed_task:
  id: IMP-25
  title: 为 `/issues/[id]` 直达页补最小 route seam 回归测试以锁定显式 back-link contract
  status: done
  summary: >-
    已为 legacy `/issues/[id]` 直达 issue 详情页补最小 route seam 回归测试：直接调用 route 并渲染返回元素，锁定其在可解析 workspace slug 时向 `IssueDetailPage` 显式透传 `/[workspace]/issue/[identifier]/[slug]` 的 `href` 与 `label: null` contract；同时覆盖 organizations 无匹配 slug 时回退为空 back-link props 的安全降级行为。
  tests:
    - cd frontend && pnpm test -- --run src/lib/routes.test.tsx: passed (5 files, 53 tests)
    - cd frontend && npx tsc --noEmit: passed
    - git diff --check: passed
  review:
    code: approved
  commit: 2cb5c35dd4be33c88ec15f4ca46c87a2a7f61fe9
  next_task: IMP-26
current_task:
  id: IMP-26
  parent_task: linear-parity-push
  title: 为 legacy `/issues/[id]` route 补 API 失败场景 seam 测试以锁定 try/catch 降级 contract
  lane: Implementation
  status: pending
  done_when: >-
    为 legacy `/issues/[id]` 直达 route 新增最小 seam 回归测试，覆盖 `getIssue(...)` 或 `getOrganizations()` 抛错时 route 仍以空 back-link props 渲染 `IssueDetailPage` 的 try/catch 降级 contract。
  blocker: '-'
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml 是 linear parity cron 的唯一恢复状态源；task-board 是 lane 明细与后继任务定义源。
    - 每轮先检查 git status/branch/head/log，再读取本状态文件、task-board 与 roadmap。
    - 每次只执行一个 execution unit；完成或阻塞后必须写回状态，并立即停止。
    - Implementation 完成闭环顺序：feature/work commit → 记录 hash → 更新状态/task-board/logbook/worktime → docs/state commit → 检查工作区干净 → push。
    - Capture 完成闭环顺序：证据产物落盘 → 更新状态/task-board/logbook/worktime → 如需状态提交则单独提交 → stop。
  next_run_prompt_summary: >-
    恢复本状态文件，优先只处理当前 lane 对应的 current_task；若是 capture 则继续 CAP-08 的同一入口校验/反馈态只读采集，若是 implementation 则根据 task-board 只推进 IMP-22 的一个最小可交付增量；完成或阻塞后必须写回状态并停止。
