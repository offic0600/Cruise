# Linear Parity 任务板

> 目的：作为 Linear 对标工作的统一状态源，但按 **采集（capture）** 与 **实现（implementation）** 两条 cron 分轨推进。
> 当前约束：
> - `cruise-linear-parity-capture` 只处理采集类任务
> - `cruise-linear-parity-push` 只处理实现类任务
> - 两条任务线都必须更新本文件，但只能推进各自所属 lane 的任务
> - `docs/status/roadmap-state.yaml` 是 cron 恢复状态源；本文件负责 lane 明细、最小增量定义与后继任务衔接

## 状态说明

- `pending`：未开始
- `in_progress`：进行中，可继续推进
- `blocked`：当前有明确阻塞，需记录 blocker
- `done`：已完成

## Schema（very short）

```yaml
status: one of [pending, in_progress, blocked, done]
depends_on: comma-separated IDs or -
blocker: '-' when none
```

## 调度规则（machine-friendly）

```yaml
state_source:
  primary: docs/status/roadmap-state.yaml
  lane_detail_source: docs/linear-parity/task-board.md
  required_reads:
    - AGENTS.md
    - docs/planning/dev-logbook.md
    - doc/worktime.md
    - docs/status/roadmap-state.yaml
    - docs/plans/2026-04-16-linear-parity-roadmap.md
recovery_checks:
  - git status --short
  - git branch --show-current
  - git rev-parse HEAD
  - git log --oneline -5
selection:
  capture:
    lane: Capture
    pick: first(in_progress) || first(pending)
    skip_blocked_if_independent_next_exists: true
  push:
    lane: Implementation
    pick: first(in_progress) || first(pending)
    skip_blocked_if_independent_next_exists: true
planning_rules:
  next_task_required_when_done: true
  next_task_format:
    - add exactly one new pending item immediately after the done item when a natural next implementation/capture step exists
    - title must describe the next concrete increment, not a vague phase label
    - done_when must be a verifiable completion standard
    - blocker must be '-' unless currently blocked
  in_progress_must_include_next_increment_hint: true
  if_selected_item_has_no_concrete_next_increment:
    - split_it_into_smaller_follow_up_items_before_next_run
    - do_not_leave_lane_in_plan_only_loop
execution_model:
  one_execution_unit_per_run: true
  closure_after_single_task: true
  special_run_types:
    - closure-only
    - repair
    - push-only
constraints:
  capture_forbidden:
    - Cruise 产品代码实现
  push_forbidden:
    - 截图
    - DOM 取证
    - HAR 取证
    - 浏览器只读探测
run_rules:
  min_real_increment_minutes: 5
  max_real_increment_minutes: 15
  plan_only_forbidden: true
  blocked_requires_blocker_note: true
  dirty_tree_requires_scope_judgement: true
closure_order:
  - feature/work commit
  - record real commit hash
  - update state/task docs
  - docs/state commit
  - confirm clean working tree
  - git push origin HEAD
update_after_each_run:
  - docs/linear-parity/task-board.md
  - docs/planning/dev-logbook.md
  - doc/worktime.md
  - docs/status/roadmap-state.yaml
  - task_related_artifacts
delivery:
  push_done_requires_commit_and_push: true
  push_git_author: offic0600 <offic0600@163.com>
report_format:
  - 本轮完成的唯一 task
  - 关键改动文件
  - 测试/验证结果
  - review 结论
  - commit SHA
  - push 结果
  - 更新后的 task-board 摘要
  - 下一轮应执行的 task
```

---

## Capture lane（只给 `cruise-linear-parity-capture`）

| id | title | status | depends_on | done_when | blocker |
|---|---|---|---|---|---|
| CAP-01 | 稳定记录 9222 元信息与可访问性现状 | done | - | `/json/version`、`/json/list`、目标页标题/URL 的可用性与波动情况已落盘到 `har/` / `flows/` 文档 | - |
| CAP-02 | 建立 Linear 页面/路由/领域文档骨架 | done | CAP-01 | `README.md`、`page-inventory.md`、`route-map.md`、`domain-model.md`、`api-catalog.md`、`ui-style-guide.md` 已存在 | - |
| CAP-03 | 补齐 Active issues 网络监听摘要 | done | CAP-01 | 已获得 `Network.requestWillBeSent/responseReceived` 事件样本，并落盘到 `docs/linear-parity/har/2026-04-18-active-issues-network-events.json` | - |
| CAP-04 | 补 Active issues DOM/结构证据摘要 | done | CAP-01 | 已新增 `docs/linear-parity/dom/active-issues-structure-summary.md`，沉淀 team Active issues 的结构级证据摘要 | - |
| CAP-05 | 建立全功能 1:1 采集计划 | done | CAP-02 | 已新增 `docs/linear-parity/full-parity-capture-plan.md`，明确页面 × 控件 × 状态 × 流程的采集口径 | - |
| CAP-06 | 枚举 Active issues 顶部工具栏与 tabs 的交互证据 | blocked | CAP-03, CAP-04, CAP-05 | 为 Active issues 页的顶部 tabs / 搜索 / filter / display / sort / new 等控件逐项补前后状态、截图/DOM/网络摘要，并新增对应 flow 文档 | authenticated page/session reuse failure：10:01 CST 最新复测中，browser 侧 `json/list` 仍可读且继续枚举到 6 个 live page targets + 1 个 worker target，其中 Linear target=`Cleantrack › Active issues`（id=`81A3713C33BCA35B2A0B8C7D177F43AD`）；但 terminal 直连 `/json/version` 与 `/json/list` 继续返回 `502`。browser 打开目标 URL 仍先落到 `Link opened in the Linear app` 中转页；本轮再次通过显式点击 `Open here instead` 稳定复现登录页分支，且仍未见 CAPTCHA/iframe，因此文档页继续只应归类为 `Learn more` / 激活歧义分支，而不是新 blocker 类型。下一最小任务仍应优先改做 page websocket/CDP 对该 target 的只读取证。证据见 `docs/linear-parity/har/2026-04-19-1001-browser-terminal-metadata-discrepancy-and-auth-blocked.json`、`docs/linear-parity/flows/active-issues-auth-session-blocker-2026-04-19-1001.md` |
| CAP-07 | 采集一个 issue 详情页的页面级与控件级证据 | done | CAP-06 | 至少拿到 issue 详情页默认态截图、DOM、可见模块、主要操作入口与网络摘要 | 2026-04-19 14:19 CST 本轮开始前已先读取 `~/Desktop/Cruise/.hermes/linear-9222-watchdog/last-status.txt`，其中 `consumer_policy=attached_cdp_only`、`metadata_layer=ok`、`websocket_attach=ok`，因此严格只复用已附着成功的本地 Chrome 9222 target `81A3713C33BCA35B2A0B8C7D177F43AD`。通过清代理后复查 `/json/list` 与 page websocket/CDP，再次确认 issue detail 页面 `CLE-28 Views 页面补齐创建/编辑/删除与状态反馈` 仍为 `readyState=complete`；在 14:08 已补默认态整页截图、可见模块与主要操作入口枚举的基础上，本轮继续对同一 target 执行 8 秒只读 `Network.requestWillBeSent/responseReceived` 监听，结果 `request_count=0`、`response_count=0`，由此把“默认态 network-idle”沉淀为明确网络摘要结论，满足 CAP-07 done_when。证据见 `docs/linear-parity/flows/issue-detail-page-cdp-screenshot-and-controls-2026-04-19-1408.md`、`docs/linear-parity/flows/issue-detail-page-cdp-network-idle-2026-04-19-1416.md`、`docs/linear-parity/har/2026-04-19-1408-issue-detail-screenshot-and-controls-summary.json`、`docs/linear-parity/har/2026-04-19-1416-issue-detail-network-idle-summary.json`、`docs/linear-parity/screenshots/issue-detail-page-2026-04-19-1408.png` |
| CAP-08 | 采集 issue 创建/编辑入口的打开态与校验态证据 | blocked | CAP-06 | 只读优先，允许打开弹窗/抽屉并记录字段、默认值、校验反馈，不做持久提交 | 2026-04-21 08:22 CST 再次复核 `~/Desktop/Cruise/.hermes/linear-9222-watchdog/last-status.txt`，仍明确显示 `consumer_policy=attached_cdp_only`、`metadata_layer=ok`、`websocket_attach=ok`，且附着 target 继续指向 issue detail 页面 `CLE-28 Views 页面补齐创建/编辑/删除与状态反馈`，因此本轮禁止使用 Hermes 内置 `browser_*`，也不存在“附着失败”类 blocker。随后核对 `tmp/linear-capture/summary.json` 最近时间仍为 2026-04-19 23:20:06 CST，且早于 `docs/linear-parity/task-board.md`、`docs/status/roadmap-state.yaml`、`docs/planning/dev-logbook.md`、`doc/worktime.md` 当前最近写回，说明 CAP-08 相关 capture 产物仍已完成 docs 闭环，本轮 execution unit 仅需补一次 capture lane 状态复核，不应重复采集。当前 blocker 继续是工作树闭环风险：状态文件仍停在 Implementation lane，且当前分支最新提交与 dev-logbook/worktime 都在持续收口 implementation 任务；若此时继续新增 capture 落盘，将无法保证后续 docs/state/worktime/dev-logbook 仍能按 capture 单 lane 边界安全收口。下一轮建议优先在独立干净工作树执行 CAP-08 的只读采集，或待 implementation 侧完成收口并重新对齐 capture 状态后，再继续同一 `Add label` search input 的空结果/建议反馈态证据。 |
| CAP-09 | 采集 Projects 域页面与关键控件证据 | pending | CAP-08 | 至少完成 Projects 列表页的页面级截图、DOM、主要按钮/筛选/详情入口摘要 | - |
| CAP-10 | 采集 Views 域页面与关键控件证据 | pending | CAP-05 | 至少完成 Views 列表/入口级证据，明确创建/编辑/删除入口形态 | - |
| CAP-11 | 采集 Cycles / Roadmap 域页面证据 | pending | CAP-05 | 至少完成一个 Cycles 或 Roadmap 页面级证据包 | - |
| CAP-12 | 采集 Inbox / Settings 必要子集证据 | pending | CAP-05 | 至少完成 Inbox 与一个 Settings 子页的页面级证据包 | - |

---

## Implementation lane（只给 `cruise-linear-parity-push`）

| id | title | status | depends_on | done_when | blocker |
|---|---|---|---|---|---|
| IMP-01 | 完成 Cruise vs Linear issues 域差距分析 | done | CAP-02 | `gap-analysis.md` 与 `implementation-roadmap.md` 已形成基线 | - |
| IMP-02 | 建立长期任务状态驱动机制 | done | IMP-01 | 已新增 `task-board.md`，cron 改为按任务状态推进 | - |
| IMP-03 | 细化 `/issues` 页顶部工具栏首个 UI gap | done | IMP-01 | 已形成 `active-issues-toolbar-gap.md` 执行说明 | - |
| IMP-04 | 落地 `/issues` 顶部工具栏一个最小对标改动 | done | IMP-03 | 已完成顶部 tabs / 主次操作层级的最小 UI 对标 | - |
| IMP-05 | 评估 team/workspace 语义列表路由壳层 | done | IMP-01 | 已补 `teamIssuesPath(...)` 与 team 语义路由壳层 | - |
| IMP-06 | 让 team-active Display 分组折叠切换真实作用于列表渲染 | done | IMP-05 | `collapsed=` URL 状态已真正驱动 group section 展开/折叠 | - |
| IMP-07 | 为 issue-workbench 视图归类规则补齐回归测试 | done | IMP-06 | 已补 helper 级回归测试 | - |
| IMP-08 | 优化 team-active 搜索/筛选空态反馈 | done | IMP-06, IMP-07 | 已补差异化空态文案与测试 | - |
| IMP-09 | 为 team 路由 helpers 补语义化 backlog/done 回归测试 | done | IMP-05 | 已补 `teamIssuesPath(...)` 回归测试 | - |
| IMP-10 | 为 team-active Display 折叠状态补可见反馈文案 | done | IMP-06, IMP-08 | 已补 collapsed group summary 与测试 | - |
| IMP-11 | 为 team-active 工具栏补最小可见排序状态 | done | IMP-06, IMP-08, IMP-10 | 已把 `sort=` URL 状态与可见摘要接通 | - |
| IMP-12 | 让 team-active 列表真实消费 sort URL 状态并输出稳定排序 | done | IMP-11 | `sort=` 已真实驱动 Active issues workbench rows 的 `updatedAt / manual` 顺序，且已补 `issue-workbench` helper 与 row builder 回归测试并通过定向 `vitest` | 若后续排序规则依赖更多 Linear 证据，由 capture lane 补充；本任务基于现有证据已完成安全最小实现 |
| IMP-13 | 为 team-active 工具栏补最小可见 filter summary 聚合反馈 | done | IMP-08, IMP-12 | 已将 team-active filter 按钮与侧栏摘要升级为首项 + 余量聚合反馈文案，并补充 `filterSummaryLabel(...)` 回归测试且定向 `vitest` 通过 | `npx tsc --noEmit` 仍受仓库既有 `MarkdownEditor.tsx` tiptap 缺依赖与 `issue-view.test.ts` 旧字面量类型不收窄影响，未全绿 |
| IMP-14 | 为 team-active 高级筛选补最小 labels URL/摘要闭环 | done | IMP-13 | 已让 `labelIds=` URL 状态进入 team-active filter draft / query / summary 闭环，并补充 helper 回归测试且定向 `vitest` 通过 | `npx tsc --noEmit` 仍受仓库既有 Vitest/路径别名/JSX 配置与 `MarkdownEditor.tsx` 依赖问题影响，未能作为全局绿灯 |
| IMP-15 | 收紧 team-active 多条件筛选摘要为首项 + 余量聚合文案 | done | IMP-14 | 已将 `filterSummaryLabel(...)` 改为首项 + 余量聚合，并让页内 search summary 复用同一套短摘要逻辑；补充 `summarizeFilterTokens(...)` helper 与回归测试且定向 `vitest` 通过 | `npx tsc --noEmit` 仍受仓库既有 `MarkdownEditor.tsx` tiptap 依赖缺失影响 |
| IMP-16 | 为 team-active 工具栏状态说明补 helper 级回归测试 | done | IMP-15 | 已导出 `searchStatusText(...)` / `noteText(...)` / `sortSummaryLabel(...)` 为可测试 seam，并补齐中英文回归测试且定向 `vitest` 通过 | `npx tsc --noEmit` 仍受仓库既有 `MarkdownEditor.tsx` tiptap 依赖缺失影响，且当前组件仍保留既有 `IN_PROGRESS` / “过滤条件” 字面量 |
| IMP-17 | 评估 issue 详情页 route shell 与页面骨架落地切口 | done | CAP-07 | 已完成 issue detail route shell 与详情页骨架/空态 contract 的一轮最小收口：route 复用 `buildIssueDetailRouteBackLink(...)`、`buildIssueDetailRouteEmptyStateModel(...)` 与 `buildIssueDetailRouteEmptyStateLinkHref(...)` 统一 not-found/error/back-CTA 语义，`IssueDetailPage` 加载态切到稳定双栏骨架，并修正 DONE/CANCELED + resolution 徽标文案连接符；routes seam 同时锁定 visible CTA raw branch 与 normalized model 的 href 一致性，且本轮已在当前环境完成 `pnpm test -- --run src/lib/routes.test.tsx`、`npx tsc --noEmit` 与 `git diff --check` 验证通过。 | - |
| IMP-18 | 为 issue detail route shell 补共享 skeleton/empty-state contract 的下一刀验证收口 | done | IMP-17 | 已新增 `buildIssueDetailRouteShellModel(...)`，把 route shell 的 empty-state model 与 loading-state defaults/overrides 收口到同一 helper，并补 routes seam 回归测试锁定默认/显式 loading contract；定向 `pnpm test -- --run src/lib/routes.test.tsx`（5 files, 50 tests）、`npx tsc --noEmit`、`git diff --check` 均通过 | - |
| IMP-19 | 让 issue detail route 真正消费共享 shell model 渲染 loading/empty-state 分支 | done | IMP-18 | issue detail route 的 loading/background-refetch/error/no-id 分支现已消费 `buildIssueDetailRouteShellModel(...)` 产出的共享 shell model，删除 route 内部重复 empty-state 组装，并保持 scope 仅限 route 分支 seam；定向 `pnpm test -- --run src/lib/routes.test.tsx`（5 files, 50 tests）、`npx tsc --noEmit`、`git diff --check` 均通过 | - |
| IMP-20 | 继续收口 issue detail route 与 IssueDetailPage 之间剩余 skeleton/empty-state seam | done | IMP-19 | route shell 现已补齐 `pageBackLink` 共享 contract，并让 steady / cached-refetch 两个 `IssueDetailPage` 分支统一消费 `buildIssueDetailRouteShellModel(...)` 产出的返回链接语义；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 51 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过 | - |
| IMP-21 | 收口 issue detail route 对 IssueDetailPage 默认 back-link props 的重复组装 | done | IMP-20 | issue detail route 的默认 page 分支现已与 cached-refetch 分支一样直接展开 `buildIssueDetailRouteShellModel(...)` 产出的共享 `pageBackLink` props，删除 route 内对 `backHref/backLabel` 的重复组装；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 51 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过 | - |
| IMP-22 | 让 IssueDetailPage 直接消费共享 back-link contract props | done | IMP-21 | `IssueDetailPage` 现已把返回链接 props 收口为与 route shell `pageBackLink` 一致的 `href/label` contract，删除组件层对 `backHref/backLabel` 旧命名的专用接口，并同步更新 routes seam mock/断言；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 51 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过 | - |
| IMP-23 | 评估并收紧 IssueDetailPage 内部 workspace/team 默认 back-link fallback 的保留边界 | done | IMP-22 | 已确认 `IssueDetailPage` 的直接调用面只剩 workspace issue detail route、`/issues/[id]` 直达页与 Inbox embedded 详情；其中 Inbox 因 `embedded` 不渲染返回链接，`/issues/[id]` 直达页仍依赖内部 fallback。基于该调用面，本轮已把顶部返回链接收紧为仅消费显式传入的 `href`，并把“为什么还不能完全删掉 `teamActivePath(...)` fallback”写回状态：当前它仍只对删除后跳转与 `/issues/[id]` 直达页未显式传参场景必要。 | - |
| IMP-24 | 为 `/issues/[id]` 直达 issue 详情页补最小显式 back-link contract | done | IMP-23 | `/issues/[id]` 直达页现已在服务端先获取 issue 与 organizations，按 `organizationId` 解析 workspace slug 后显式构造并透传 `IssueDetailPage` 的 `href/label` back-link contract；顶部返回链接因此不再依赖组件内部默认 fallback，而内部 `teamActivePath(...)` 兜底仅保留给删除后跳转。定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 | - |
| IMP-25 | 为 `/issues/[id]` 直达页补最小 route seam 回归测试以锁定显式 back-link contract | done | IMP-24 | 已为 legacy `/issues/[id]` 直达 route 补最小 seam 回归测试：直接调用 route 并渲染返回元素，锁定可解析 workspace slug 时向 `IssueDetailPage` 透传 `/[workspace]/issue/[identifier]/[slug]` 的 `href` 与 `label: null` contract，同时覆盖 organizations 无匹配 slug 时安全降级为空 back-link props。定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 53 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 | - |
| IMP-26 | 为 legacy `/issues/[id]` route 补 API 失败场景 seam 测试以锁定 try/catch 降级 contract | done | IMP-25 | 已为 legacy `/issues/[id]` 直达 route 补齐 API 失败场景 seam 回归测试：分别覆盖 `getIssue(...)` 与 `getOrganizations()` 抛错时 route 仍吞掉异常并以空 back-link props 渲染 `IssueDetailPage` 的 try/catch 降级 contract。定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 55 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 | - |
| IMP-27 | 评估 legacy `/issues/[id]` route 是否可抽出共享 issue/org lookup seam 以消除重复 Promise 组装 | done | IMP-26 | 已将 legacy `/issues/[id]` route 内的 `Promise.all(getIssue, getOrganizations)` 与 workspace slug→back-link 组装提炼为 `loadIssueDetailRouteLookup(...)` / `buildIssueDetailRouteBackLinkProps(...)`，并补 routes seam 回归测试锁定共享 contract。 | - |
| IMP-28 | 为 legacy `/issues/[id]` lookup seam 补最小 route 级失败/成功 contract 断言去除 route 与 helper 断言重复 | done | IMP-27 | 已将 lookup seam 成功场景测试收紧为“先断言 `loadIssueDetailRouteLookup(...)` 返回值，再复用同一 lookup 结果断言 `buildIssueDetailRouteBackLinkProps(...)` contract”，去掉 helper 级重复手写 organizations fixture，同时保留 route 级成功/无 slug/API 失败 contract 断言。定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 | - |
| IMP-29 | 评估 legacy `/issues/[id]` route 是否可将失败断言收口为共享空 back-link contract helper | done | IMP-28 | 已确认 `getIssue(...)` / `getOrganizations()` 两条 API 失败断言仍重复表达同一空 back-link 降级 contract，并提炼测试专用 `expectLegacyIssueRouteEmptyBackLinkContract()` helper 收口共享期望；各自上游调用断言保持不变，定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 | - |
| IMP-30 | 评估 legacy `/issues/[id]` route 无 slug 空 back-link 断言是否可与失败 helper 进一步收口 | done | IMP-29 | 已确认 legacy `/issues/[id]` route 当前“无 workspace slug”route 级空 back-link 断言与 IMP-29 新增的失败分支共享 helper 表达的是同一降级 contract；本轮直接让 route 无 slug 场景复用 `expectLegacyIssueRouteEmptyBackLinkContract()`，保留 helper 级 `buildIssueDetailRouteBackLinkProps(...)` 无 slug 空对象断言不变，定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 | - |
| IMP-31 | 评估 legacy `/issues/[id]` route 空 back-link helper 是否可继续收口 route/helper 命名与调用边界 | done | IMP-30 | 已确认 legacy `/issues/[id]` route 当前 helper 级无 slug 空对象断言与 route 级空 props 断言仍共享同一“空 back-link”语义边界；本轮提炼 `expectEmptyLegacyIssueDetailRouteBackLinkProps()` 作为 helper 级唯一断言入口，并让 `expectLegacyIssueRouteEmptyBackLinkContract()` 在 route 级断言前先复用该 helper，从而继续收紧命名/调用边界，同时保持 helper 返回空对象 contract 与 route 渲染空 props contract 的层级分离。 | - |
| IMP-32 | 评估 legacy `/issues/[id]` route 空 back-link helper 命名是否可进一步对齐 legacy/route 语义 | done | IMP-31 | 已将 helper 级空 back-link 断言重命名为 `expectLegacyIssueDetailRouteEmptyBackLinkProps()`，使其与 route 级 `expectLegacyIssueRouteEmptyBackLinkContract()` 共享同一 legacy/route 词序，只保留 helper 与 contract 的层级差异，从而进一步收紧命名语义而不扩大实现面；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 | - |
| IMP-33 | 评估 legacy `/issues/[id]` route 空 back-link 测试文案是否还可继续统一为 shared legacy lookup seam 语义 | done | IMP-32 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 相关空 back-link 测试标题/描述是否仍混用 route、shared legacy lookup seam 与 workspace slug 语义；若可行，则只做一刀测试文案收口并保持定向验证通过。 | 已将 workspace slug miss 场景的 route 级测试标题改为明确表达“legacy route 透传 shared legacy lookup seam 的空 back-link contract”，从而把 route / lookup seam / miss path 的语义统一到同一句文案中；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-34 | 评估 legacy `/issues/[id]` route 空 back-link helper/contract 测试命名是否还可继续去掉重复 empty back-link 短语 | done | IMP-33 | 在不扩大实现面的前提下，复核 `expectLegacyIssueDetailRouteEmptyBackLinkProps()` 与 `expectLegacyIssueRouteEmptyBackLinkContract()` 及其调用测试标题是否仍存在可安全压缩的重复 empty/back-link 词组；若可行，则只做一刀测试命名收口并保持定向验证通过。 | 已将 helper 级与 route 级共享测试 helper 分别收口为 `expectLegacyIssueDetailRouteEmptyProps()` 与 `expectLegacyIssueRouteEmptyPropsContract()`，并把 helper 级无 slug 场景标题同步压缩为“empty props contract”，去掉重复的 empty back-link 词组；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-35 | 评估 legacy `/issues/[id]` route API 失败场景测试标题是否还可继续统一为 shared empty props contract 语义 | done | IMP-34 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 的 `getIssue(...)` / `getOrganizations()` 失败场景测试标题是否仍混用 empty back-link 与 empty props contract 语义；若可行，则只做一刀测试文案收口并保持定向验证通过。 | 已将 `getIssue(...)` 与 `getOrganizations()` 两条 API 失败场景测试标题统一改为“falls back to the shared empty props contract ...”，去掉残留的 empty back-link wording，同时保留 `expectLegacyIssueRouteEmptyPropsContract()` 共享断言与原有调用校验不变；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-36 | 评估 legacy `/issues/[id]` route workspace slug miss 场景测试标题是否还可继续统一为 shared empty props contract 语义 | done | IMP-35 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 当前 workspace slug miss 场景 route 级测试标题是否仍保留 empty back-link wording；若可行，则只做一刀测试文案收口，使其与 shared empty props contract 语义一致并保持定向验证通过。 | 已将 workspace slug miss 场景 route 级测试标题统一改为 `falls back to the shared empty props contract when the legacy /issues/[id] route workspace slug lookup misses`，去掉 shared legacy lookup seam / empty back-link wording，同时保留共享 `expectLegacyIssueRouteEmptyPropsContract()` 断言与原有运行时逻辑不变；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-37 | 评估 legacy `/issues/[id]` route helper 级 workspace slug miss 场景测试标题是否还可继续统一为 shared empty props contract 语义 | done | IMP-36 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route helper 级 workspace slug miss 场景测试标题 `falls back to an empty props contract when the shared legacy lookup seam cannot resolve a workspace slug` 是否仍可安全收口为 shared empty props contract 语义；若可行，则只做一刀测试文案收口并保持定向验证通过。 | 已将 helper 级 workspace slug miss 场景测试标题统一改为 `falls back to the shared empty props contract when the shared legacy lookup seam cannot resolve a workspace slug`，补上残留 `shared` wording，同时保持共享 `expectLegacyIssueDetailRouteEmptyProps()` helper、断言结构与运行时逻辑不变；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-38 | 评估 legacy `/issues/[id]` route helper/route 两个 shared empty props contract 场景测试标题是否还可继续收口 shared legacy lookup seam wording | done | IMP-37 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route helper 级与 route 级两个 shared empty props contract 场景测试标题中保留的 `shared legacy lookup seam` wording 是否仍可安全继续收口；若可行，则只做一刀测试文案收口并保持定向验证通过。 | 已将 helper 级 workspace slug miss 场景测试标题从 `falls back to the shared empty props contract when the shared legacy lookup seam cannot resolve a workspace slug` 收口为 `falls back to the shared empty props contract when the legacy lookup seam cannot resolve a workspace slug`，去掉重复的 `shared legacy lookup seam` wording，同时保持共享 `expectLegacyIssueDetailRouteEmptyProps()` helper、route 级空 props contract 与运行时逻辑不变；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-39 | 评估 legacy `/issues/[id]` route route 级 workspace slug miss 场景测试标题是否还可继续去掉 legacy /issues/[id] 重复路径 wording | done | IMP-38 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route route 级 workspace slug miss 场景测试标题 `falls back to the shared empty props contract when the legacy /issues/[id] route workspace slug lookup misses` 是否仍可安全去掉重复的 `legacy /issues/[id] route` 路径 wording；若可行，则只做一刀测试文案收口并保持定向验证通过。 | 已将 route 级 workspace slug miss 场景测试标题从 `falls back to the shared empty props contract when the legacy /issues/[id] route workspace slug lookup misses` 收口为 `falls back to the shared empty props contract when the workspace slug lookup misses`，去掉重复路径 wording，同时保持 `expectLegacyIssueRouteEmptyPropsContract()` 共享断言、helper/route 运行时逻辑与断言结构不变；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-40 | 评估 legacy `/issues/[id]` route helper/route 两个 workspace slug miss 场景测试标题是否还可继续统一 lookup miss wording | done | IMP-39 | 在不扩大实现面的前提下，复核 helper 级 `cannot resolve a workspace slug` 与 route 级 `workspace slug lookup misses` 两个 workspace slug miss 场景测试标题是否仍可安全统一为更一致的 lookup miss wording；若可行，则只改第一处可安全收口的测试文案并保持定向验证通过。 | 已将 helper 级 workspace slug miss 场景测试标题从 `falls back to the shared empty props contract when the legacy lookup seam cannot resolve a workspace slug` 收口为 `falls back to the shared empty props contract when the workspace slug lookup misses`，使 helper 级与 route 级两个 workspace slug miss 场景统一为相同 lookup miss wording，同时保持共享 `expectLegacyIssueDetailRouteEmptyProps()` / `expectLegacyIssueRouteEmptyPropsContract()` 断言、fixture 设置以及 helper/route 运行时逻辑不变；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-41 | 评估 legacy `/issues/[id]` route helper/route 两个 shared empty props contract workspace slug miss 场景测试标题是否还可继续去掉重复 wording | done | IMP-40 | 在不扩大实现面的前提下，复核 helper/route 两个已统一为 `workspace slug lookup misses` 的 shared empty props contract 场景测试标题是否仍存在可安全压缩的重复 wording；若可行，则只改第一处可安全收口的测试文案并保持定向验证通过。 | 已将 helper 级 workspace slug miss 场景测试标题从 `falls back to the shared empty props contract when the workspace slug lookup misses` 收口为 `falls back to the empty props contract when the workspace slug lookup misses`，去掉与 helper 名称 `expectLegacyIssueDetailRouteEmptyProps()` 重复表达的 `shared` wording，同时保持 route 级标题、共享断言、fixture 设置以及 helper/route 运行时逻辑不变；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-42 | 评估 legacy `/issues/[id]` route route 级 shared empty props contract workspace slug miss 场景测试标题是否还可继续去掉重复 wording | done | IMP-41 | 在不扩大实现面的前提下，复核 route 级已使用 `shared empty props contract` 的 workspace slug miss 场景测试标题是否仍存在可安全压缩的重复 wording；若可行，则只改这一处 route 级测试文案并保持定向验证通过。 | 已将 route 级 workspace slug miss 场景测试标题从 `falls back to the shared empty props contract when the workspace slug lookup misses` 收口为 `falls back to the empty props contract when the workspace slug lookup misses`，去掉与 route 级共享空 props contract 断言重复表达的 `shared` wording，同时保持 helper 级标题、共享断言、fixture 设置以及 helper/route 运行时逻辑不变；定向 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`（5 files, 57 tests）、`cd frontend && npx tsc --noEmit`、`git diff --check` 均通过。 |
| IMP-43 | 评估 legacy `/issues/[id]` route helper/route 两个 empty props contract workspace slug miss 场景测试标题是否仍需保留并拆分 wording | done | IMP-42 | 在不扩大实现面的前提下，复核 helper/route 两个现已统一为 `empty props contract when the workspace slug lookup misses` 的场景测试标题是否仍存在必须保留的层级区分；若可行，则只新增一个更具体的下一刀实现子任务，并保持定向验证或文档核对边界清晰。 | 已确认 helper 级场景是在直接断言 `buildIssueDetailRouteBackLinkProps(...)` 的空对象 contract，而 route 级场景是在渲染 legacy `/issues/[id]` route 后断言 `IssueDetailPage` 收到空 props contract；本轮因此只将 route 级标题收口为 `renders the empty props contract when the workspace slug lookup misses`，用 `renders` 明确 route 渲染层语义，同时保持 helper 级标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-44 | 评估 legacy `/issues/[id]` route helper 级 workspace slug miss 场景测试标题是否需要补上 helper lookup 语义以避免与 route 级 render wording 混淆 | done | IMP-43 | 在不扩大实现面的前提下，复核 helper 级场景标题是否应从当前泛化的 `falls back to the empty props contract when the workspace slug lookup misses` 收口为能显式表达 helper lookup seam 的 wording（例如强调 helper/lookup 而非 route render）；若可行，则只改 helper 级这一处标题并保持定向验证通过。 | 已将 helper 级 workspace slug miss 场景测试标题从 `falls back to the empty props contract when the workspace slug lookup misses` 收口为 `falls back to the empty props contract when the helper workspace slug lookup misses`，用 `helper workspace slug lookup` 明确 helper seam 语义，同时保持 route 级 `renders the empty props contract when the workspace slug lookup misses` 标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-45 | 评估 legacy `/issues/[id]` route helper/route 两个 empty props contract workspace slug miss 场景测试标题是否还需进一步统一 falls back / renders 动词风格 | done | IMP-44 | 在不扩大实现面的前提下，复核 helper/route 两个 workspace slug miss empty props contract 场景当前分别使用 `falls back` 与 `renders` 动词是否仍需进一步统一或稳定；若可行，则只新增一个更具体的下一刀实现子任务或只改第一处安全的测试文案，并保持定向验证通过。 | 已复核 helper/route 两个 workspace slug miss 场景的层级差异：helper 级直接断言 `buildIssueDetailRouteBackLinkProps(...)` 返回空对象 contract，route 级则渲染 legacy `/issues/[id]` route 后断言 `IssueDetailPage` 收到空 props contract；因此本轮不统一 helper/route 动词，而是仅将 helper 级标题收口为 `returns the empty props contract when the helper workspace slug lookup misses`，用 `returns` 明确 helper 返回值语义，同时保持 route 级 `renders ...` 标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-46 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题中的 shared empty props contract wording 是否需按 helper/route 层级进一步统一动词 | done | IMP-45 | 在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 legacy `/issues/[id]` route API 失败场景当前 `falls back to the shared empty props contract ...` 标题是否仍需按 helper/route 层级统一为更稳定的 route 动词；若可行，则只改第一处安全的测试文案或新增一个更具体的下一刀实现子任务，并保持定向验证通过。 | 已复核两条 API 失败场景都属于 route 渲染层断言：legacy `/issues/[id]` route 渲染后校验 `IssueDetailPage` 收到共享空 props contract，而非 helper 返回值 contract；因此本轮仅将 `getIssue(...)` 失败场景标题从 `falls back ...` 收口为 `renders the shared empty props contract when getIssue rejects in the legacy /issues/[id] route`，用 `renders` 明确 route 语义，同时保持 `getOrganizations()` 失败标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-47 | 评估 legacy `/issues/[id]` route `getOrganizations()` 失败场景测试标题是否也应按 route render 语义从 falls back 收口为 renders | done | IMP-46 | 在不扩大实现面的前提下，复核 `getOrganizations()` 失败场景当前 `falls back to the shared empty props contract when getOrganizations rejects in the legacy /issues/[id] route` 标题是否应与 `getIssue(...)` 失败场景一样按 route render 层收口为 `renders ...`；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认该场景与上一轮 `getIssue(...)` 失败场景一样属于 route 级渲染后断言 `IssueDetailPage` 收到共享空 props contract，而非 helper 返回值 contract；因此已将标题收口为 `renders the shared empty props contract when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-48 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均改为 renders 后是否仍需保留 shared wording | done | IMP-47 | 在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 legacy `/issues/[id]` route API 失败场景当前均为 `renders the shared empty props contract ...` 后，标题中的 `shared` wording 是否仍有必要；若可行，则只改第一处安全标题或明确保留理由，并保持定向验证通过。 | 已复核两条 API 失败场景当前都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props contract 的观察面；由于共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 已在命名中表达该 contract 为共享入口，因此本轮仅将 `getIssue(...)` 失败场景标题收口为 `renders the empty props contract when getIssue rejects in the legacy /issues/[id] route`，去掉重复的 `shared` wording，同时保持 `getOrganizations()` 失败标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-49 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 shared wording | done | IMP-48 | 在不扩大实现面的前提下，复核 `getOrganizations()` 失败场景当前 `renders the shared empty props contract when getOrganizations rejects in the legacy /issues/[id] route` 标题中的 `shared` wording 是否也可像 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处标题并保持定向验证通过。 | 已确认该场景与上一轮 `getIssue(...)` 失败场景一样属于 route 级渲染后断言 `IssueDetailPage` 收到空 props contract，而非 helper 返回值 contract；因此已将标题收口为 `renders the empty props contract when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-50 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 shared wording 后是否仍需保留 empty props contract 全称 | done | IMP-49 | 在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 API 失败场景当前均为 `renders the empty props contract ...` 后，标题中的 `empty props contract` 全称是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核两条 API 失败场景当前都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 与上下文已表达 contract 语义的前提下，本轮仅将 `getIssue(...)` 失败场景标题收口为 `renders the empty props when getIssue rejects in the legacy /issues/[id] route`，去掉 `contract` wording，同时保持 `getOrganizations()` 失败标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-51 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 contract wording | done | IMP-50 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前仍为 `renders the empty props contract ...` 时，标题中的 `contract` wording 是否也可像 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核该场景与上一轮 `getIssue(...)` 失败场景一样，都是 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 与上下文已表达 contract 语义的前提下，因此已将标题收口为 `renders the empty props when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-52 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 contract wording 后是否仍需保留 empty props 全称 | done | IMP-51 | 在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 API 失败场景当前均为 `renders the empty props ...` 后，标题中的 `empty props` 全称是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核两条 API 失败场景都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 仍承载 contract 语义的前提下，本轮仅将 `getIssue(...)` 失败场景标题收口为 `renders the empty state props when getIssue rejects in the legacy /issues/[id] route`，以更直接表达空态 props 语义，同时保持 `getOrganizations()` 失败标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-53 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应从 empty props 收口为 empty state props | done | IMP-52 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders the empty props when getOrganizations rejects in the legacy /issues/[id] route` 标题是否应与已收口的 `getIssue(...)` 场景一致改为 `renders the empty state props ...`；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认该场景与上一轮 `getIssue(...)` 失败场景一样属于 route 级渲染后断言 `IssueDetailPage` 收到空态 props 的观察面；因此已将标题收口为 `renders the empty state props when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-54 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均改为 empty state props 后是否仍需保留 state 全称 | done | IMP-53 | 在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 API 失败场景当前均为 `renders the empty state props ...` 后，标题中的 `state` wording 是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核两条 API 失败场景当前虽都属于 route 级渲染后断言 `IssueDetailPage` 收到空态 props 的观察面，但在共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 已承载 contract 语义、且 `getOrganizations()` 场景仍保留 `empty state props` wording 作为同组对照的前提下，本轮仅将 `getIssue(...)` 失败场景标题收口为 `renders the empty props when getIssue rejects in the legacy /issues/[id] route`，去掉 `state` wording，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-55 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 state wording | done | IMP-54 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders the empty state props when getOrganizations rejects in the legacy /issues/[id] route` 标题中的 `state` wording 是否也可像 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核该场景与上一轮 `getIssue(...)` 失败场景一样，都是 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 已承载 contract 语义、且 `getIssue(...)` 场景已先行去掉 `state` wording 的前提下，因此已将标题收口为 `renders the empty props when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-56 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均改为 empty props 后是否仍需保留 the 全称 | done | IMP-55 | 在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 API 失败场景当前均为 `renders the empty props ...` 后，标题中的 `the` wording 是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核两条 API 失败场景测试标题当前都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在第二条 `getOrganizations()` 标题仍保留 `the` wording 作为对照、且共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 已承载 contract 语义的前提下，本轮仅将 `getIssue(...)` 失败场景标题收口为 `renders empty props when getIssue rejects in the legacy /issues/[id] route`，同时保持第二条失败标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-57 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 the wording | done | IMP-56 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders the empty props when getOrganizations rejects in the legacy /issues/[id] route` 标题中的 `the` wording 是否也可像上一轮 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核该场景与上一轮 `getIssue(...)` 对称场景一样，都是 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在上一轮已安全去掉第一条标题中的 `the` wording、且共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 已承载 contract 语义的前提下，因此已将标题收口为 `renders empty props when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-58 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 the wording 后是否仍需保留 empty props 全称 | done | IMP-57 | 在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 API 失败场景当前均为 `renders empty props ...` 后，标题中的 `empty props` 全称是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核两条 API 失败场景都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在两条标题都已稳定为 `renders empty props ...`、且共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 已承载 contract 语义的前提下，本轮仅将 `getIssue(...)` 失败场景标题收口为 `renders props when getIssue rejects in the legacy /issues/[id] route`，同时保持 `getOrganizations()` 失败标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-59 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 empty wording | done | IMP-58 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders empty props when getOrganizations rejects in the legacy /issues/[id] route` 标题中的 `empty` wording 是否也可像 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核该场景与上一轮 `getIssue(...)` 对称场景一样，当前都属于 route 级渲染后断言 `IssueDetailPage` 收到 props 的观察面；在上一轮已安全去掉第一条标题中的 `empty` wording、且共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 继续承载 contract 语义的前提下，因此已将标题收口为 `renders props when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-60 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 empty wording 后是否仍需保留 props 全称 | done | IMP-59 | 在不扩大实现面的前提下，复核 `getIssue(...)` / `getOrganizations()` 两条 API 失败场景当前均为 `renders props when ...` 后，标题中的 `props` wording 是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核两条 API 失败场景当前都属于 route 级渲染后断言 `IssueDetailPage` 收到 props 的观察面；在两条标题都已稳定为 `renders props when ...`、且共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 继续承载 contract 语义的前提下，本轮仅将 `getIssue(...)` 失败场景标题收口为 `renders when getIssue rejects in the legacy /issues/[id] route`，同时保持 `getOrganizations()` 失败标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-61 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 props wording | done | IMP-60 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders props when getOrganizations rejects in the legacy /issues/[id] route` 标题中的 `props` wording 是否也可像 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核该场景与上一轮 `getIssue(...)` 对称场景一样，属于 route 级渲染后断言 `IssueDetailPage` 收到 props 的观察面；在上一轮已安全去掉第一条失败标题中的 `props` wording、且共享断言 helper `expectLegacyIssueRouteEmptyPropsContract()` 继续承载 contract 语义的前提下，因此已将标题收口为 `renders when getOrganizations rejects in the legacy /issues/[id] route`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-62 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 props wording 后是否仍需保留 route 全称 | done | IMP-61 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `renders when ... in the legacy /issues/[id] route` 后，标题中的 `legacy /issues/[id] route` 全称是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核两条 API 失败场景测试标题当前都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在文件作用域 describe 已限定 legacy `/issues/[id]` route 上下文、且同组对称标题 `renders when getOrganizations rejects in the legacy /issues/[id] route` 仍保留完整路径 wording 作为对照的前提下，本轮仅将第一条 `getIssue(...)` 失败场景标题从 `renders when getIssue rejects in the legacy /issues/[id] route` 收口为 `renders when getIssue rejects`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-63 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 route 全称 | done | IMP-62 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders when getOrganizations rejects in the legacy /issues/[id] route` 标题中的 `legacy /issues/[id] route` 全称是否也可像 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认该场景与上一轮 `getIssue(...)` 对称场景一样，当前都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在文件作用域 describe 已限定 legacy `/issues/[id]` route 上下文、且上一轮第一条标题 `renders when getIssue rejects` 已证明去掉 route 全称不会损失辨识度的前提下，因此已将标题收口为 `renders when getOrganizations rejects`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-64 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 route 全称后是否仍需保留 when wording | done | IMP-63 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `renders when getIssue rejects` / `renders when getOrganizations rejects` 后，标题中的 `when` wording 是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 legacy `/issues/[id]` route 两条 API 失败场景当前都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在同一 describe 已限定 legacy `/issues/[id]` route 上下文、且对称第二条标题 `renders when getOrganizations rejects` 仍保留 `when` wording 作为对照的前提下，本轮仅将第一条 `getIssue(...)` 失败场景标题从 `renders when getIssue rejects` 收口为 `renders getIssue rejects`，以验证去掉第一处 `when` 连接词后标题仍保持可辨识，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-65 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 when wording | done | IMP-64 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders when getOrganizations rejects` 标题中的 `when` wording 是否也可像上一轮 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核该场景当前属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在同一 describe 已限定 legacy `/issues/[id]` route 上下文、且上一轮第一条对称标题 `renders getIssue rejects` 已证明去掉 `when` 连接词后标题仍保持可辨识的前提下，因此已将标题收口为 `renders getOrganizations rejects`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-66 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 when wording 后是否仍需保留 renders 动词 | done | IMP-65 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `renders getIssue rejects` / `renders getOrganizations rejects` 后，标题中的 `renders` 动词是否仍存在可安全收口的一处；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核两条 API 失败场景当前都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在同一 describe 已限定 legacy `/issues/[id]` route 上下文、且第二条对称标题 `renders getOrganizations rejects` 仍保留 `renders` 动词作为对照的前提下，本轮仅将第一条 `getIssue(...)` 失败场景标题从 `renders getIssue rejects` 收口为 `getIssue rejects`，验证去掉第一处 route render 动词后标题仍保持可辨识，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-67 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 renders 动词 | done | IMP-66 | 在不扩大实现面的前提下，复核 `getOrganizations()` API 失败场景当前 `renders getOrganizations rejects` 标题中的 `renders` 动词是否也可像上一轮 `getIssue(...)` 场景一样安全去掉；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认该场景与上一轮 `getIssue(...)` 对称场景一样，当前都属于 route 级渲染后断言 `IssueDetailPage` 收到空 props 的观察面；在同一 describe 已限定 legacy `/issues/[id]` route 上下文、且上一轮第一条标题 `getIssue rejects` 已证明去掉 `renders` 动词后标题仍保持可辨识的前提下，因此已将标题收口为 `getOrganizations rejects`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-68 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 renders 动词后是否仍需保留 route 级语义词 | done | IMP-67 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `getIssue rejects` / `getOrganizations rejects` 后，是否仍需要补回或保留某个 route 级语义词来维持测试标题辨识度；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核若完全去掉 route 级语义词，会与 helper 级 lookup 标题层次更接近；因此本轮仅将第一条 route 级 API 失败场景测试标题补成 `route getIssue rejects`，以最小 route 级语义词恢复标题辨识度，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-69 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应补回 route 级语义词 | done | IMP-68 | 在不扩大实现面的前提下，复核 `getOrganizations rejects` 标题是否应与本轮已补回 route 级语义词的 `route getIssue rejects` 保持对称，也补成一条最小 route 级语义词标题；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认 `getOrganizations()` API 失败场景与上一轮 `route getIssue rejects` 一样属于 legacy `/issues/[id]` route 渲染层测试标题；为保持两条 route 级 API 失败场景的最小对称辨识度，本轮仅将该标题补成 `route getOrganizations rejects`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-70 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均补回 route 级语义词后是否仍需保留 `route` 前缀 | done | IMP-69 | 已复核同组 helper 级标题仍通过 `returns ... helper workspace slug lookup misses` 等 wording 保留层级区分，route 级两条 API 失败场景在 describe 已限定 legacy `/issues/[id]` route 上下文下，无需继续保留 `route` 前缀维持辨识度；本轮仅将第一条标题从 `route getIssue rejects` 收口为 `getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | - |
| IMP-71 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应去掉 `route` 前缀 | done | IMP-70 | 已确认第二条 route 级 API 失败场景与上一轮 `getIssue rejects` 一样处于 describe 已限定的 legacy `/issues/[id]` route 上下文内，因此无需继续保留 `route` 前缀维持辨识度；本轮仅将标题从 `route getOrganizations rejects` 收口为 `getOrganizations rejects`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | - |
| IMP-72 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 `route` 前缀后是否仍需保留 route 级语义词区分 helper 层级 | done | IMP-71 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `getIssue rejects` / `getOrganizations rejects` 后，是否仍需要补回或替换某个更稳妥的 route 级语义词来维持与 helper 级 lookup 标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义，而 route 级 API 失败场景若继续保持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过于接近；因此本轮仅将第一条 route 级 API 失败场景测试标题从 `getIssue rejects` 补回为 `route getIssue rejects`，以最小 route 级语义词恢复层级辨识度，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-73 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应补回 route 级语义词 | done | IMP-72 | 在不扩大实现面的前提下，复核 `getOrganizations rejects` 标题是否应与本轮已补回 route 级语义词的 `route getIssue rejects` 保持对称，也补成一条最小 route 级语义词标题；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核 `getOrganizations()` API 失败场景与上一轮已补回 route 级语义词的 `route getIssue rejects` 一样，仍属于 legacy `/issues/[id]` route 渲染层测试标题；若继续保持 `getOrganizations rejects`，会破坏两条 route 级 API 失败场景对 helper 级标题的最小对称层级区分。因此本轮仅将第二条 route 级 API 失败场景测试标题从 `getOrganizations rejects` 补回为 `route getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-74 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均补回 route 级语义词后是否仍需保留对称前缀 | done | IMP-73 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `route getIssue rejects` / `route getOrganizations rejects` 后，是否仍需要继续保留对称的 `route` 前缀维持与 helper 级标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，两条 route 级 API 失败场景若继续保持 `route getIssue rejects` / `route getOrganizations rejects` 会造成多余的对称前缀。因此本轮仅将第一条 route 级 API 失败场景测试标题从 `route getIssue rejects` 收口为 `getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-75 | 评估 legacy `/issues/[id]` route 第二条 API 失败场景测试标题在首条已去掉 `route` 前缀后是否也应对称收口 | done | IMP-74 | 在不扩大实现面的前提下，复核 `route getOrganizations rejects` 标题是否应与本轮已收口为 `getIssue rejects` 的第一条 route 级 API 失败场景保持对称，也去掉 `route` 前缀；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条 route 级失败标题保持对称，也去掉 `route` 前缀。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-76 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 `route` 前缀后是否仍需补回 route 级语义词 | done | IMP-75 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `getIssue rejects` / `getOrganizations rejects` 后，是否仍需要重新补回某个 route 级语义词来维持与 helper 级 lookup 标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义，而 route 级 API 失败场景若继续保持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过于接近；因此本轮仅将第一条 route 级 API 失败场景测试标题从 `getIssue rejects` 补回为 `route getIssue rejects`，以最小 route 级语义词恢复层级辨识度，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-77 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应补回 route 级语义词 | done | IMP-76 | 在不扩大实现面的前提下，复核 `getOrganizations rejects` 标题是否应与本轮已补回 route 级语义词的 `route getIssue rejects` 保持对称，也补成一条最小 route 级语义词标题；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认 `getOrganizations()` API 失败场景与上一轮 `route getIssue rejects` 一样属于 legacy `/issues/[id]` route 渲染层测试标题；为保持两条 route 级 API 失败场景的最小对称辨识度，本轮仅将该标题补成 `route getOrganizations rejects`，同时保持共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-78 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均补回 route 级语义词后是否仍需保留对称前缀 | done | IMP-77 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `route getIssue rejects` / `route getOrganizations rejects` 后，是否仍需要继续保留对称的 `route` 前缀维持与 helper 级标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，两条 route 级 API 失败场景若继续保持 `route getIssue rejects` / `route getOrganizations rejects` 会造成多余的对称前缀。因此本轮仅将第一条 route 级 API 失败场景测试标题从 `route getIssue rejects` 收口为 `getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-79 | 评估 legacy `/issues/[id]` route 第二条 API 失败场景测试标题在首条已去掉 `route` 前缀后是否也应对称收口 | done | IMP-78 | 在不扩大实现面的前提下，复核 `route getOrganizations rejects` 标题是否应与本轮已收口为 `getIssue rejects` 的第一条 route 级 API 失败场景保持对称，也去掉 `route` 前缀；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条 route 级失败标题保持对称，也去掉 `route` 前缀。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-80 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 `route` 前缀后是否仍需补回 route 级语义词 | done | IMP-79 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `getIssue rejects` / `getOrganizations rejects` 后，是否仍需要重新补回某个 route 级语义词来维持与 helper 级 lookup 标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义，而 route 级 API 失败场景若继续保持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过于接近；因此本轮仅将第一条 route 级 API 失败场景测试标题从 `getIssue rejects` 补回为 `route getIssue rejects`，以最小 route 级语义词恢复层级辨识度，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-81 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应补回 route 级语义词 | done | IMP-80 | 在不扩大实现面的前提下，复核 `getOrganizations rejects` 标题是否应与本轮已补回 route 级语义词的 `route getIssue rejects` 保持对称，也补成一条最小 route 级语义词标题；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认 `getOrganizations()` API 失败场景与上一轮已补回 route 级语义词的 `route getIssue rejects` 一样，仍属于 legacy `/issues/[id]` route 渲染层测试标题；若继续保持 `getOrganizations rejects`，会破坏两条 route 级 API 失败场景对 helper 级标题的最小对称层级区分。因此本轮仅将第二条 route 级 API 失败场景测试标题从 `getOrganizations rejects` 补回为 `route getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-82 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均补回 route 级语义词后是否仍需保留对称前缀 | done | IMP-81 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `route getIssue rejects` / `route getOrganizations rejects` 后，是否仍需要继续保留对称的 `route` 前缀维持与 helper 级标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，两条 route 级 API 失败场景若继续保持 `route getIssue rejects` / `route getOrganizations rejects` 会造成多余的对称前缀。因此本轮仅将第一条 route 级 API 失败场景测试标题从 `route getIssue rejects` 收口为 `getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 76090df7031e5daa5f05ff1bf87b194c50f82ec6. |
| IMP-83 | 评估 legacy `/issues/[id]` route 第二条 API 失败场景测试标题在首条已去掉 `route` 前缀后是否也应对称收口 | done | IMP-82 | 在不扩大实现面的前提下，复核 `route getOrganizations rejects` 标题是否应与本轮已收口为 `getIssue rejects` 的第一条 route 级 API 失败场景保持对称，也去掉 `route` 前缀；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条 route 级失败标题保持对称，也去掉 `route` 前缀。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-84 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 `route` 前缀后是否仍需补回 route 级语义词 | done | IMP-83 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `getIssue rejects` / `getOrganizations rejects` 后，是否仍需要重新补回某个 route 级语义词来维持与 helper 级 lookup 标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义，而 route 级 API 失败场景若继续同时保持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过于接近；因此本轮仅将第一条 route 级 API 失败场景测试标题从 `getIssue rejects` 补回为 `route getIssue rejects`，以最小 route 级语义词恢复层级辨识度，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 21ac02997d0468b4a6dcd5a2b81386fe0cf8fca4. |
| IMP-85 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应补回 route 级语义词 | done | IMP-84 | 在不扩大实现面的前提下，复核 `getOrganizations rejects` 标题是否应与本轮已补回 route 级语义词的 `route getIssue rejects` 保持对称，也补成一条最小 route 级语义词标题；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认 `getOrganizations()` API 失败场景与上一轮已补回 route 级语义词的 `route getIssue rejects` 一样，仍属于 legacy `/issues/[id]` route 渲染层测试标题；若继续保持 `getOrganizations rejects`，会破坏两条 route 级 API 失败场景对 helper 级标题的最小对称层级区分。因此本轮仅将第二条 route 级 API 失败场景测试标题从 `getOrganizations rejects` 补回为 `route getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: c20b123c1e270cb81179889157c9829daac6b508. |
| IMP-86 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均补回 route 级语义词后是否仍需保留对称前缀 | done | IMP-85 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `route getIssue rejects` / `route getOrganizations rejects` 后，是否仍需要继续保留对称的 `route` 前缀维持与 helper 级标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，两条 route 级 API 失败场景若继续保持 `route getIssue rejects` / `route getOrganizations rejects` 会造成多余的对称前缀。因此本轮仅将第一条 route 级 API 失败场景测试标题从 `route getIssue rejects` 收口为 `getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 53d8e0869aaf41d63cdef5edd4be55f3c253e815. |
| IMP-87 | 评估 legacy `/issues/[id]` route 第二条 API 失败场景测试标题在首条已去掉 `route` 前缀后是否也应对称收口 | done | IMP-86 | 在不扩大实现面的前提下，复核 `route getOrganizations rejects` 标题是否应与本轮已收口为 `getIssue rejects` 的第一条 route 级 API 失败场景保持对称，也去掉 `route` 前缀；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条 route 级失败标题保持对称，也去掉 `route` 前缀。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 628de1ba03c80005efd8414a1b18fd3cd7599bd9. |
| IMP-88 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 `route` 前缀后是否仍需补回 route 级语义词 | done | IMP-87 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `getIssue rejects` / `getOrganizations rejects` 后，是否仍需要重新补回某个 route 级语义词来维持与 helper 级 lookup 标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义，而 route 级 API 失败场景若继续同时保持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过于接近；因此本轮仅将第一条 route 级 API 失败场景测试标题从 `getIssue rejects` 补回为 `route getIssue rejects`，以最小 route 级语义词恢复层级辨识度，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: a15fa6f0ab494e50369b6d5ed54a5d75144fdc74. |
| IMP-89 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应补回 route 级语义词 | done | IMP-88 | 在不扩大实现面的前提下，复核 `getOrganizations rejects` 标题是否应与本轮已补回 route 级语义词的 `route getIssue rejects` 保持对称，也补成一条最小 route 级语义词标题；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认 `getOrganizations()` API 失败场景与本轮前已补回 route 级语义词的 `route getIssue rejects` 一样，仍属于 legacy `/issues/[id]` route 渲染层测试标题；若继续保持 `getOrganizations rejects`，会破坏两条 route 级 API 失败场景对 helper 级标题的最小对称层级区分。因此本轮仅将第二条 route 级 API 失败场景测试标题从 `getOrganizations rejects` 补回为 `route getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: b017e526e464c766ef4a7e29ff7dd5ca34578028. |
| IMP-90 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均补回 route 级语义词后是否仍需保留对称前缀 | done | IMP-89 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `route getIssue rejects` / `route getOrganizations rejects` 后，是否仍需要继续保留对称的 `route` 前缀维持与 helper 级标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，两条 route 级 API 失败场景即使都带有 route 级语义，也不必继续保持完全对称的 `route` 前缀来维持层级区分。因此本轮仅将第一条标题从 `route getIssue rejects` 收口为 `getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 5600390e10d847b43bf7872239aaf7d21a5c4488. |
| IMP-91 | 评估 legacy `/issues/[id]` route 第二条 API 失败场景测试标题在首条已去掉 `route` 前缀后是否也应对称收口 | done | IMP-90 | 在不扩大实现面的前提下，复核 `route getOrganizations rejects` 标题是否应与本轮已收口为 `getIssue rejects` 的第一条 route 级 API 失败场景保持对称，也去掉 `route` 前缀；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条 route 级失败标题保持对称，也去掉 `route` 前缀。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: f3c6310f14197cf9f62e31ec823b1e7f57088215. |
| IMP-92 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 `route` 前缀后是否仍需补回 route 级语义词 | done | IMP-91 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `getIssue rejects` / `getOrganizations rejects` 后，是否仍需要补回或替换某个更稳妥的 route 级语义词来维持与 helper 级 lookup 标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义，而 route 级 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，会与 helper 层级重新过于接近。因此本轮仅将第一条 route 级 API 失败场景测试标题从 `getIssue rejects` 补回为 `route getIssue rejects`，以最小 route 级语义词恢复层级辨识度，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 7e8146e21b7a41ddddce4956c5b13882b945b16e. |
| IMP-93 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应补回 route 级语义词 | done | IMP-92 | 在不扩大实现面的前提下，复核 `getOrganizations rejects` 标题是否应与本轮已补回 route 级语义词的 `route getIssue rejects` 保持对称，也补成一条最小 route 级语义词标题；若可行，则只改这一处安全标题并保持定向验证通过。 | 已确认 `getOrganizations()` API 失败场景与本轮前已补回 route 级语义词的 `route getIssue rejects` 一样，仍属于 legacy `/issues/[id]` route 渲染层测试标题；若继续保持 `getOrganizations rejects`，会破坏两条 route 级 API 失败场景对 helper 级标题的最小对称层级区分。因此本轮仅将第二条 route 级 API 失败场景测试标题从 `getOrganizations rejects` 补回为 `route getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: dbdd761b01a010d3ee8cece151b5e7195325085d. |
| IMP-94 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均补回 route 级语义词后是否仍需保留对称前缀 | done | IMP-93 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `route getIssue rejects` / `route getOrganizations rejects` 后，是否仍需要继续保留对称的 `route` 前缀维持与 helper 级标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，两条 route 级 API 失败场景即使都补回了 route 级语义，也不必继续保持完全对称的 `route` 前缀来维持层级区分。因此本轮仅将第一条 route 级 API 失败场景测试标题从 `route getIssue rejects` 收口为 `getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 277d31c62713efd966fddc17bacdca212358a6d8. |
| IMP-95 | 评估 legacy `/issues/[id]` route 第二条 API 失败场景测试标题在首条已去掉 `route` 前缀后是否也应对称收口 | done | IMP-94 | 在不扩大实现面的前提下，复核 `route getOrganizations rejects` 标题是否应与本轮已收口为 `getIssue rejects` 的第一条 route 级 API 失败场景保持对称，也去掉 `route` 前缀；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条 route 级失败标题保持对称，也去掉 `route` 前缀。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 4b96165af35e3f4f6decaef3e13b9d9e00ac535c. |
| IMP-96 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在均去掉 `route` 前缀后是否仍需补回 route 级语义词 | done | IMP-95 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均已收口为 `getIssue rejects` / `getOrganizations rejects` 后，是否仍需要重新补回某个 route 级语义词来维持与 helper 级 lookup 标题的层级区分；若可行，则只改第一处安全标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义，而 route 级 API 失败场景若继续同时保持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过于接近。因此本轮仅将第一条 route 级 API 失败场景测试标题从 `getIssue rejects` 补回为 `route getIssue rejects`，以最小 route 级语义词恢复层级辨识度，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: efde12dc53915e47c4a81bb5e81c57f7ea7ca738. |
| IMP-97 | 评估 legacy `/issues/[id]` route `getOrganizations()` API 失败场景测试标题是否也应补回 route 级语义词 | done | IMP-96 | 在不扩大实现面的前提下，复核 `getOrganizations rejects` 标题是否应与本轮已补回 route 级语义词的 `route getIssue rejects` 保持对称，也补成一条最小 route 级语义词标题；若可行，则只改这一处安全标题并保持定向验证通过。 | 已复核在第一条 route 级 API 失败场景已恢复为 `route getIssue rejects` 后，第二条若继续保留 `getOrganizations rejects` 会破坏两条 route 级 API 失败标题的最小对称层级区分；因此本轮仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变。commit `374f735011d73eefe374e1ca6ece86e93c2f26da`。 |
| IMP-98 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在再次均带 route 级语义词后是否仍需保留第二条对称前缀 | done | IMP-97 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留 route 级对称前缀；若可行，则只将第二条安全收口回 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文下，第二条 route 级 API 失败场景 `route getOrganizations rejects` 已无须继续保留 route 对称前缀来维持层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 8bcc2481e30dc9c2c6b33cbf865aa0a2897461f1. |
| IMP-99 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-98 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留 route 级语义词以维持与 helper 级 lookup 标题的最小层级区分；若可行，则只将第一条安全收口回 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已收口为 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留 route 级语义词来维持层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 17618a0002a8cae696a8fb50c0b0481bbc69eb54. |
| IMP-100 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-99 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前均为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 071a09bfa769123750534beec31d45c867ad7d1b. |
| IMP-101 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-100 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 仍需要恢复最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 1cc3dc678bb593acfd9b79195bf5ce49efb664cc. |
| IMP-102 | 评估 legacy `/issues/[id]` route 两条 API 失败场景测试标题在再次均带 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-101 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 3ab341667a3c9ed6c51fe6d528b93ced13ba0127. |
| IMP-103 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-102 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 17a4ef557f322aa6d760851c00f019e05b4b3331. |
| IMP-104 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-103 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 4046b1cda2fc7a8fdaa2f9319e510023b7ac4209. |
| IMP-105 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-104 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 仍需要恢复最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 1bfd1f28f2405929020eaddc57761decae9925ed. |
| IMP-106 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-105 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 72bf3df36db60d3e29e485cbe0eac85967961426. |
| IMP-107 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-106 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 4464214b152c01ddef20398b01b6cdb7b05c90fc. |
| IMP-108 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-107 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: f6e08c2bfce40ba0b2fa71019e66c5b67871838a. |
| IMP-109 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-108 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 仍需要恢复最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: f23c455b70b1c9dcbb624086fb250bed7dfb309e. |
| IMP-110 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-109 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: e244633a49773ffbcd1f7be8f9032dc5984919bc. |
| IMP-111 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-110 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: d6bcef1d5f0464e8a0d33aa920638b49407eec17. |
| IMP-112 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-111 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: ca4e567db7bdf97f72ac11413b9de68f1a7c5dbf. |
| IMP-113 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-112 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 仍需要恢复最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 780492ecf2012c01f8240a8209806a2c56c7fa73. |
| IMP-114 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-113 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 06eb17d4dc2f14e860014bc27182e8f45e930801. |
| IMP-115 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-114 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: dbea78695eba7d786695f3dda0b11ad755f06d1a. |
| IMP-116 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-115 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 9a8cf97b039b7cf5ffc4275dae2b29a235865da7. |
| IMP-117 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-116 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近，因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 77a88c4e92be9731cb1143e26264c71caa8fef81. |
| IMP-118 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-117 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: c8ed42bd6384d6b18d45a31744a7f3ae0e2b490a. |
| IMP-119 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-118 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: d430e5aa3f39a03627922bbe8d9a7b63ded03c64. |
| IMP-120 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-119 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: f5bf9b3eaf599a1ecc12e3722999feee02a2b3d3. |
| IMP-121 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-120 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 47f9d96a7eacd9b6cf68469f78d85031f9467a8d. |
| IMP-122 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-121 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 86426360c9a21bf6acf365cd153940da6d76127a. |
| IMP-123 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-122 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 010af594ff8913cb08dff06d4510d9fbc34e5497. |
| IMP-124 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-123 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 9d78d5415405d2f7193ab10c93555331ff6cd40a. |
| IMP-125 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-124 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 7bbb113aa1dc67ff17318a67281f5681f053db9f. |
| IMP-126 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-125 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 792c9ef5e31d6a531fb720dd8041d2d0f1758d28. |
| IMP-127 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-126 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: c1a81823d90fb96fa48df1ae4c7ebc84264394a1. |
| IMP-128 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-127 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 272899da43d7341b9adca2a45f15663f0bcbaf4c. |
| IMP-129 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-128 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 841542f1c4bc754e233187ab2bd3883d85f16254. |
| IMP-130 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时恢复 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-129 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 79f05be1f38ee764967db2bc5949298361432845. |
| IMP-131 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-130 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: d41c42ac9e858ad02b7db09a1eef553523e0bfd1. |
| IMP-132 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-131 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 85328ce69f946ef9ea11d2510617fc679ecb9f34. |
| IMP-133 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-132 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 5ba6d0f0d57e68af35bef8138e2f0b07e29456a8. |
| IMP-134 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时恢复 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-133 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: d31dd505cb4f44918a269651145c2689d0b514ff. |
| IMP-135 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-134 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: fcdbfcbda8a3c7ddee778df2b823a9ca8e161cc3. |
| IMP-136 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-135 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: a426174564efc1fe557f628bd36ac1793bcddd4f. |
| IMP-137 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-136 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 38d0a1ca592ee09074c8627607262efcfdbe66bb. |
| IMP-138 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时恢复 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-137 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 464c97b5168874b2f65f3eeba84bf284900416d2. |
| IMP-139 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-138 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 0186436b98d475718e179d3640ddac1aace32a6b. |
| IMP-140 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-139 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近，因此仅将其补回为 `route getOrganizations rejects`，同时保持第一条标题、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 |
| IMP-141 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需恢复 route 级语义词 | done | IMP-140 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近，因此仅将其补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: a7e369bc7cc06b468c191277798dbc48af04193e. |
| IMP-142 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时恢复 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-141 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: a2ffc5c0235e28ffe53b7e60e5d4597c3c405def. |
| IMP-143 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-142 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: b096b63acea6d026b343a5fd43918f4bb00c1709. |
| IMP-144 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-143 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: d5f437ff61c1f67b232915ecc104c5757ebe2ca8. |
| IMP-145 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需恢复 route 级语义词 | done | IMP-144 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带最小 route 级语义词，会与 helper 级 lookup 标题层级过于接近，因此仅将其补回为 `route getIssue rejects`，保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变；验证：`cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、`cd frontend && npx tsc --noEmit`、`git diff --check`。 |
| IMP-146 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时恢复 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-145 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条仍为 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题安全收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: ea42a706c2b5b705af6627d044c0a2e3ef36809e. |
| IMP-147 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-146 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条仍为 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题安全收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 5bcdd1f8c016dd1a939e4d8dd21d5bfe7e6b39aa. |
| IMP-148 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-147 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 19162da372aec2098a07b858cf6a32ba1b0d4a66. |
| IMP-149 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-148 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带最小 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 2663739ae96d00471cb8918d764187fdd9399fa5. |
| IMP-150 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时恢复 route wording 后第二条是否仍需保留 route 级语义词 | done | IMP-149 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 9fb3e4bed6262e7f8de023743e23dd397e212e63. |
| IMP-151 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-150 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: 570598205392a783d47cbd7dd42447b72ed561f1. |
| IMP-152 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-151 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 commit: b6cfd61c0f1dfdae088b685fe548bbd5cf77fbc0. |
| IMP-153 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-152 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 route/helper 两层标题层级不对称，因此本轮仅将其补回为 `route getIssue rejects`，其余共享断言、fixture 设置与 helper/route 运行时逻辑保持不变。 | 72cca43a9bc1c2cf1fe6ea593dca76ae30b859d7 |

| IMP-154 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时恢复 route wording 后第二条是否仍可再次收口为无 route wording | done | IMP-153 | 在不扩大实现面的前提下，基于当前 `route getIssue rejects` / `route getOrganizations rejects` 组合，复核第二条是否仍可安全收口为 `getOrganizations rejects` 且继续维持与 helper 级 lookup 标题的层级区分；若可行，则仅修改第二条标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 已无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | f6bcfd9b7f83fef537a49bc44506023b7f3ba754 |

| IMP-155 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-154 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条是否仍需保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样无须继续保留最小 route 级语义词来维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，同时保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | aa9b72da92804970e4b14ab869fb5cc6d54ded38 |

| IMP-156 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-155 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `getOrganizations rejects` 后，第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第二条安全补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | e662450a59d99422a6bfe632c75e677957fb2cd2 |

| IMP-157 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-156 | 在不扩大实现面的前提下，复核 legacy `/issues/[id]` route 两条 API 失败场景当前为 `getIssue rejects` / `route getOrganizations rejects` 后，第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则只将第一条安全补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | 928097c29d390214ed67533b08851c9ee3ccad22 |

| IMP-158 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时恢复 route wording 后第二条是否仍可再次收口为无 route wording | done | IMP-157 | 在不扩大实现面的前提下，基于当前 `route getIssue rejects` / `route getOrganizations rejects` 组合，复核第二条是否仍可安全收口为 `getOrganizations rejects` 且继续维持与 helper 级 lookup 标题的层级区分；若可行，则仅修改第二条标题并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 可安全再次收口为 `getOrganizations rejects`，同时继续与 helper 级 lookup 标题保持可辨识的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，并保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | d26084c94c05f9452fd7b32f4c1c9857f8d19ceb |

| IMP-159 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍需保留 route 级语义词 | done | IMP-158 | 在不扩大实现面的前提下，基于当前 `route getIssue rejects` / `getOrganizations rejects` 组合，复核第一条是否仍需继续保留最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则仅将第一条标题再次收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样可安全再次收口为 `getIssue rejects`，同时继续与 helper 级 lookup 标题保持可辨识的层级区分。因此本轮仅将该标题收口为 `getIssue rejects`，并保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | 8ca4a772bca48480604638d26ca252e7eebc3a35 |

| IMP-160 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-159 | 在不扩大实现面的前提下，基于当前 `getIssue rejects` / `getOrganizations rejects` 组合，复核第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则仅将第二条标题补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | 74b6850bd9782f3717803afb1769a9bde8e9497e |
| IMP-161 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否需要恢复 route 级语义词 | done | IMP-160 | 在不扩大实现面的前提下，基于当前 `getIssue rejects` / `route getOrganizations rejects` 组合，复核第一条是否需要恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若可行，则仅将第一条标题补回为 `route getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | 59bd0b99c17ef4c92f8a2ce2ee243d6b7eb0c4c8 |
| IMP-162 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route wording 后第二条是否仍可收口为无 route wording | done | IMP-161 | 在不扩大实现面的前提下，基于当前 `route getIssue rejects` / `route getOrganizations rejects` 组合，复核第二条是否仍可安全收口为 `getOrganizations rejects` 且继续维持与 helper 级 lookup 标题的层级区分；若可行，则仅将第二条标题收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 仍可安全收口为 `getOrganizations rejects`，同时继续维持与 helper 级 lookup 标题的层级区分。因此本轮仅将该标题收口为 `getOrganizations rejects`，同时保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | 778140831bc4d2c307e169a379a9c5eec77d3b0b |
| IMP-163 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条是否仍可收口为无 route wording | done | IMP-162 | 在不扩大实现面的前提下，基于当前 `route getIssue rejects` / `getOrganizations rejects` 组合，复核第一条是否仍可安全收口为 `getIssue rejects` 且继续维持与 helper 级 lookup 标题的层级区分；若可行，则仅将第一条标题收口为 `getIssue rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 同样可安全收口为 `getIssue rejects`，同时继续维持与 helper 级 lookup 标题的层级区分。因此本轮仅将第一条标题收口为 `getIssue rejects`，并保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | 7a87390764e338572809eba65653da1ed669f710 |
| IMP-164 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为无 route wording 后第二条是否仍需恢复 route 级语义词 | done | IMP-163 | 在不扩大实现面的前提下，基于当前 `getIssue rejects` / `getOrganizations rejects` 组合，复核第二条是否仍需恢复最小 route 级语义词以维持与 helper 级 lookup 标题的层级区分；若需要，则仅将第二条标题补回为 `route getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | e9067b1ec55617bc6deab8e00a221873f05ee0a5 |
| IMP-165 | 评估 legacy `/issues/[id]` route 两条 API 失败场景在再次变为双 route wording 后第二条是否仍可收口为无 route wording | done | IMP-164 | 在不扩大实现面的前提下，基于当前 `route getIssue rejects` / `route getOrganizations rejects` 组合，复核第二条是否仍可安全收口为 `getOrganizations rejects` 且继续维持与 helper 级 lookup 标题的层级区分；若可行，则仅将第二条标题收口为 `getOrganizations rejects` 并保持定向验证通过。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `route getIssue rejects` 后，第二条 `route getOrganizations rejects` 仍可安全收口为 `getOrganizations rejects`；保持第一条 `route getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | d49f8f2e1008950fd1465cafb6c4294439530d94 |
| IMP-166 | 将第一条 route 级 API 失败标题重新收口为无 route wording，并验证 helper/route 层级仍可辨识 | done | IMP-165 | 仅将 legacy `/issues/[id]` route 的第一条 API 失败场景测试标题从 `route getIssue rejects` 收口为 `getIssue rejects`，保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变；完成后通过 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、`cd frontend && ./node_modules/.bin/tsc --noEmit` 与 `git diff --check` 验证。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `getOrganizations rejects` 后，第一条 `route getIssue rejects` 仍可安全收口为 `getIssue rejects`；保持第二条 `getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | d47761ab04dcdc8f21fae1529c0dae18a687553a |
| IMP-167 | 重新评估第二条 route 级 API 失败标题是否需要恢复 route wording，以维持 helper/route 层级区分 | done | IMP-166 | 基于当前 `getIssue rejects` / `getOrganizations rejects` 组合，复核第二条 API 失败场景标题是否仍需恢复为 `route getOrganizations rejects` 以维持与 helper 级 lookup 标题的层级区分；若需要，则仅改这一处标题并保持共享断言、fixture 设置与 helper/route 运行时逻辑不变，随后通过 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、`cd frontend && ./node_modules/.bin/tsc --noEmit` 与 `git diff --check` 验证。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第一条已维持 `getIssue rejects` 后，第二条 `getOrganizations rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题补回为 `route getOrganizations rejects`，同时保持第一条 `getIssue rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | fc39ffb39f2c7311e7cd04db35845c2758c41171 |

| IMP-168 | 重新评估第一条 API 失败标题是否需要恢复 route wording，以维持 helper/route 层级区分 | done | IMP-167 | 基于当前 `getIssue rejects` / `route getOrganizations rejects` 组合，复核第一条 API 失败场景标题是否仍需恢复为 `route getIssue rejects` 以维持与 helper 级 lookup 标题的层级区分；若需要，则仅改这一处标题并保持共享断言、fixture 设置与 helper/route 运行时逻辑不变，随后通过 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、`cd frontend && ./node_modules/.bin/tsc --noEmit` 与 `git diff --check` 验证。 | 已复核 helper 级标题仍通过 `returns the empty props contract when the helper workspace slug lookup misses` 明确保留 helper seam 语义；在 describe 已限定 legacy `/issues/[id]` route 上下文且第二条已维持 `route getOrganizations rejects` 后，第一条 `getIssue rejects` 若继续不带 route 级语义词，会与 helper 级 lookup 标题层级过于接近。因此本轮仅将该标题恢复为 `route getIssue rejects`，同时保持第二条 `route getOrganizations rejects`、共享断言、fixture 设置与 helper/route 运行时逻辑不变。 | b38af940c407f9a4b3febf8ce6fb7192e67ccdc9 |

| IMP-169 | 重新评估第一条 route 级 API 失败标题在双 route wording 恢复后是否仍可再次收口为无 route wording | pending | IMP-168 | 基于当前 `route getIssue rejects` / `route getOrganizations rejects` 组合，复核第一条 API 失败场景标题是否仍可安全收口为 `getIssue rejects` 且继续维持与 helper 级 lookup 标题的层级区分；若可行，则仅改这一处标题并保持共享断言、fixture 设置与 helper/route 运行时逻辑不变，随后通过 `cd frontend && pnpm test -- --run src/lib/routes.test.tsx`、`cd frontend && ./node_modules/.bin/tsc --noEmit` 与 `git diff --check` 验证。 | - |

## 最近一次人工调整

- 2026-04-18：新增任务板，改为“任务状态驱动”而不是“Task 1/Task 2”静态提示词驱动。
- 2026-04-18：进一步收紧为 capture / implementation 双 lane；采集 cron 与实现 cron 禁止跨 lane 抢任务。
- 2026-04-19：引入 `docs/status/roadmap-state.yaml` 作为 cron 恢复状态源，任务板退化为 lane 明细与后继任务定义源。

## Cron 执行提示（给未来运行）

- 不要重新发明任务顺序；先恢复 git 真实状态与 `docs/status/roadmap-state.yaml`，再回到本文件对应 lane。
- capture cron 只补证据，不做实现；implementation cron 只做实现，不扩展成采集。
- 若无真实 blocker，必须落一个真实增量。
- 某项任务一旦标记为 `done`，必须立刻在其后补一个新的、同 lane 的 `pending` 详细任务，写清具体切口、可验证完成标准与依赖。
- 若某个 `in_progress` 任务的 done_when 已过宽、导致连续两轮无法判断下一刀做什么，必须先拆小或补“下一最小增量”说明，不能继续空转复核。
- 每轮只做一个 execution unit；完成或阻塞后都必须写回状态并停止。
- 报告格式固定为：本轮完成的唯一 task / 关键改动文件 / 测试或验证结果 / review 结论 / commit SHA / push 结果 / 更新后的 task-board 摘要 / 下一轮应执行的 task。

