server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: cruise

  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null
    parser:
      allow-non-numeric-numbers: true
    deserialization:
      fail-on-unknown-properties: false
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

  mail:
    host: ${SPRING_MAIL_HOST:localhost}
    port: ${SPRING_MAIL_PORT:1025}
    properties:
      mail:
        smtp:
          auth: ${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:false}
          starttls:
            enable: ${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:false}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.cruise: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.hibernate: WARN

jwt:
  secret: ${JWT_SECRET:cruise-secret-key-for-jwt-token-generation-must-be-long-enough-256-bits}
  expiration: ${JWT_EXPIRATION:86400000}

cruise:
  auth:
    frontend-base-url: ${CRUISE_AUTH_FRONTEND_BASE_URL:http://localhost:3000}
    backend-base-url: ${CRUISE_AUTH_BACKEND_BASE_URL:http://localhost:8080}
    legacy-password-enabled: true
    default-organization-slug: cruise
    provider-secret-key: ${CRUISE_AUTH_PROVIDER_SECRET_KEY:cruise-provider-secret-key}
    magic-link:
      enabled: true
      token-ttl-minutes: 15
      callback-path: /login/callback
      from-address: noreply@cruise.local
    google:
      enabled: true
      provider-key: google
      protocol: OIDC
      provider-type: OIDC
      display-name: Google
      issuer-url: https://accounts.google.com
      authorization-url: https://accounts.google.com/o/oauth2/v2/auth
      token-url: https://oauth2.googleapis.com/token
      userinfo-url: https://openidconnect.googleapis.com/v1/userinfo
      jwks-url: https://www.googleapis.com/oauth2/v3/certs
      scopes: openid profile email
      is-default: false
      # Set client-id and client-secret in deployment env/config before enabling real sign-in.
    enterprise:
      enabled: false
      provider-key: enterprise
      protocol: OIDC
      provider-type: OIDC
      display-name: Enterprise SSO
      scopes: openid profile email
      is-default: false
  storage:
    attachments-root: ${CRUISE_STORAGE_ATTACHMENTS_ROOT:storage}

minimax:
  api-key: ${MINIMAX_API_KEY:}
  model: ${MINIMAX_MODEL:MiniMax-M2.5}
  base-url: ${MINIMAX_BASE_URL:https://api.minimax.chat/v1}
