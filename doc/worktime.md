# Cruise 项目工时记录

| 项目 | Session | 日期 | 工作内容 | 预估工时 | 实际工时 |
|---|---|---|---|---|---|
| Linear 对标 | Session 260 | 2026-04-21 | 按 implementation lane 完成 IMP-141：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 259 | 2026-04-21 | 按 implementation lane 完成 IMP-140：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 258 | 2026-04-21 | 按 implementation lane 完成 IMP-139：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 257 | 2026-04-21 | 按 implementation lane 完成 IMP-138：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 256 | 2026-04-21 | 按 implementation lane 完成 IMP-137：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否需要恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 255 | 2026-04-21 | 按 implementation lane 完成 IMP-136：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 254 | 2026-04-21 | 按 implementation lane 完成 IMP-135：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 253 | 2026-04-21 | 按 implementation lane 完成 IMP-134：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 252 | 2026-04-20 | 按 implementation lane 完成 IMP-133：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否需要恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 251 | 2026-04-20 | 按 implementation lane 完成 IMP-132：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 250 | 2026-04-20 | 按 implementation lane 完成 IMP-131：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 249 | 2026-04-20 | 按 implementation lane 完成 IMP-130：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 248 | 2026-04-20 | 按 implementation lane 完成 IMP-129：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否需要恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 247 | 2026-04-20 | 按 implementation lane 完成 IMP-128：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 246 | 2026-04-20 | 按 implementation lane 完成 IMP-127：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 245 | 2026-04-20 | 按 implementation lane 完成 IMP-126：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 244 | 2026-04-20 | 按 implementation lane 完成 IMP-125：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否需要恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 243 | 2026-04-20 | 按 implementation lane 完成 IMP-124：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 242 | 2026-04-20 | 按 implementation lane 完成 IMP-123：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 241 | 2026-04-20 | 按 implementation lane 完成 IMP-122：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 240 | 2026-04-20 | 按 implementation lane 完成 IMP-121：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 239 | 2026-04-20 | 按 implementation lane 完成 IMP-120：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 238 | 2026-04-20 | 按 implementation lane 完成 IMP-119：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 237 | 2026-04-20 | 按 implementation lane 完成 IMP-118：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 236 | 2026-04-20 | 按 implementation lane 完成 IMP-117：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 235 | 2026-04-20 | 按 implementation lane 完成 IMP-116：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 234 | 2026-04-20 | 按 implementation lane 完成 IMP-115：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 233 | 2026-04-20 | 按 implementation lane 完成 IMP-114：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 232 | 2026-04-20 | 按 implementation lane 完成 IMP-113：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 231 | 2026-04-20 | 按 implementation lane 完成 IMP-112：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 230 | 2026-04-20 | 按 implementation lane 完成 IMP-111：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 229 | 2026-04-20 | 按 implementation lane 完成 IMP-110：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 228 | 2026-04-20 | 按 implementation lane 完成 IMP-109：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 227 | 2026-04-20 | 按 implementation lane 完成 IMP-108：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 226 | 2026-04-20 | 按 implementation lane 完成 IMP-107：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 225 | 2026-04-20 | 按 implementation lane 完成 IMP-106：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 `route getIssue rejects` / `route getOrganizations rejects` 后，第二条 `route getOrganizations rejects` 是否仍需保留最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 224 | 2026-04-20 | 按 implementation lane 完成 IMP-105：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `getIssue rejects` / `route getOrganizations rejects` 的非对称 route wording 后，第一条 `getIssue rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 223 | 2026-04-20 | 按 implementation lane 完成 IMP-104：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 222 | 2026-04-20 | 按 implementation lane 完成 IMP-103：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 的非对称 route wording 后，第一条 `route getIssue rejects` 是否仍需保留 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 221 | 2026-04-20 | 按 implementation lane 完成 IMP-102：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route wording 后第二条 `route getOrganizations rejects` 是否仍需保留 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 220 | 2026-04-20 | 按 implementation lane 完成 IMP-101：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为非对称 route wording 后第一条 `getIssue rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且第二条已维持 `route getOrganizations rejects` 时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 219 | 2026-04-20 | 按 implementation lane 完成 IMP-100：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后，第二条 `getOrganizations rejects` 是否仍需恢复最小 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，需要补回该前缀以维持与 helper lookup 标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 218 | 2026-04-20 | 按 implementation lane 完成 IMP-99：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次变为 `route getIssue rejects` / `getOrganizations rejects` 后，第一条 `route getIssue rejects` 是否仍需保留 route 级语义词；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 217 | 2026-04-20 | 按 implementation lane 完成 IMP-98：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次均带 route 级语义词后，第二条 `route getOrganizations rejects` 是否仍需保留 route 对称前缀；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留该前缀，因此仅将其收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 216 | 2026-04-20 | 按 implementation lane 完成 IMP-97：复核 legacy `/issues/[id]` route 第二条 API 失败场景 `getOrganizations rejects` 是否应与已恢复为 `route getIssue rejects` 的第一条标题保持 route 级语义对称；确认若继续保留无 route 前缀会破坏 route 级标题的最小层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 215 | 2026-04-20 | 按 implementation lane 完成 IMP-96：复核 legacy `/issues/[id]` route 两条 API 失败场景在均收口为 `getIssue rejects` / `getOrganizations rejects` 后是否仍需补回 route 级语义词；确认两条 route 标题若同时去掉 route 语义会与 helper lookup 标题层级过近，因此仅将第一条恢复为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit，并在 closeout 中把 roadmap-state/task-board 里的 IMP-96 真实 40 位 SHA 统一修正为 `efde12dc53915e47c4a81bb5e81c57f7ea7ca738` 后准备 docs commit/push | 0.1h | 0.3h |
| Linear 对标 | Session 214 | 2026-04-20 | 按 implementation lane 完成 IMP-95：复核 legacy `/issues/[id]` route 第二条 API 失败场景 `route getOrganizations rejects` 是否应与已收口为 `getIssue rejects` 的第一条标题保持对称并去掉 `route` 前缀；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，应将其收口为 `getOrganizations rejects`，同时保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 213 | 2026-04-20 | 按 implementation lane 完成 IMP-94：复核 legacy `/issues/[id]` route 两条 API 失败场景在均补回 route 级语义词后是否仍需保留对称前缀；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留第一条 `route getIssue rejects` 的前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 212 | 2026-04-20 | 按 implementation lane 完成 IMP-93：复核 legacy `/issues/[id]` route `getOrganizations()` API 失败场景是否应与 `route getIssue rejects` 保持 route 级语义对称；确认若继续保留 `getOrganizations rejects` 会破坏与 helper 级标题的最小对称层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 211 | 2026-04-20 | 按 implementation lane 完成 IMP-92：复核 legacy `/issues/[id]` route 两条 API 失败场景在再次同时收口为 `getIssue rejects` / `getOrganizations rejects` 后是否仍需补回 route 级语义词；确认 helper seam wording 仍要求恢复最小 route 层级辨识，因此仅将第一条标题补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 210 | 2026-04-20 | 按 implementation lane 完成 IMP-90：复核 legacy `/issues/[id]` route 两条 API 失败场景在均补回 route 级语义词后是否仍需保留第一条对称前缀；确认在 describe 已限定 route 上下文且 helper seam wording 仍清晰时，无需继续保留 `route getIssue rejects` 的前缀，因此仅将其收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 209 | 2026-04-20 | 按 implementation lane 完成 IMP-89：复核 `getOrganizations()` API 失败场景是否应与 `route getIssue rejects` 保持 route 级语义对称；确认若继续保留 `getOrganizations rejects` 会削弱与 helper 级标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 208 | 2026-04-20 | 按 capture lane 复核 CAP-08：确认 watchdog 继续为 `consumer_policy=attached_cdp_only`、`websocket_attach=ok`，且 `tmp/linear-capture/summary.json` 仍早于 task-board / roadmap-state / dev-logbook / worktime 的最近写回，因此本轮不重复采集，只补写 CAP-08 的 docs 闭环与 blocker 说明，明确当前阻塞是工作树闭环风险而非附着失败 | 0.2h | 0.2h |
| Linear 对标 | Session 207 | 2026-04-20 | 按 implementation lane 完成 IMP-88：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认 route 级两条 API 失败场景若都维持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过近，因此仅将第一条 route 级失败标题补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 206 | 2026-04-20 | 按 implementation lane 完成 IMP-87：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认在 describe 已限定 legacy route 上下文下第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条标题保持对称，也去掉 `route` 前缀，因此仅将第二条标题收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 205 | 2026-04-20 | 按 implementation lane 完成 IMP-86：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认在 describe 已限定 legacy route 上下文下两条 route 级 API 失败场景继续保持对称 `route` 前缀会造成多余冗词，因此仅将第一条 route 级失败标题从 `route getIssue rejects` 收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 204 | 2026-04-20 | 按 implementation lane 完成 IMP-85：复核第二条 route 级 API 失败场景 `getOrganizations rejects` 是否应与已补回的 `route getIssue rejects` 保持最小对称 route 语义；确认若继续保留无前缀标题会削弱与 helper 级标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 203 | 2026-04-20 | 按 implementation lane 完成 IMP-84：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认 route 级两条 API 失败场景若都维持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过近，因此仅将第一条 route 级失败标题补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 202 | 2026-04-20 | 按 implementation lane 完成 IMP-83：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认在 describe 已限定 legacy route 上下文下第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条标题保持对称，也去掉 `route` 前缀，因此仅将第二条标题收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 201 | 2026-04-20 | 按 implementation lane 完成 IMP-82：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认在 describe 已限定 legacy route 上下文下两条 route 级 API 失败场景继续保持对称 `route` 前缀会造成多余冗词，因此仅将第一条 route 级失败标题从 `route getIssue rejects` 收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 200 | 2026-04-20 | 按 implementation lane 完成 IMP-81：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条标题保持对称，也去掉 `route` 前缀，因此仅将第二条标题收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 199 | 2026-04-20 | 按 implementation lane 完成 IMP-80：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认 route 级两条 API 失败场景若都维持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过近，因此仅将第一条 route 级失败标题补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 198 | 2026-04-20 | 按 implementation lane 完成 IMP-79：复核第二条 route 级 API 失败场景 `getOrganizations rejects` 是否应与已补回的 `route getIssue rejects` 保持最小对称 route 语义；确认若继续保留无前缀标题会削弱与 helper 级标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 197 | 2026-04-20 | 按 implementation lane 完成 IMP-78：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认 route 级两条 API 失败场景若都维持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过近，因此仅将第一条 route 级失败标题补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 196 | 2026-04-20 | 按 implementation lane 完成 IMP-77：复核第二条 route 级 API 失败场景 `getOrganizations rejects` 是否应与已补回的 `route getIssue rejects` 保持最小对称 route 语义；确认若继续保留无前缀标题会削弱与 helper 级标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 195 | 2026-04-20 | 按 implementation lane 完成 IMP-76：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认在 describe 已限定 legacy route 上下文下第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条标题保持对称，也去掉 `route` 前缀，因此仅将第二条标题收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 194 | 2026-04-20 | 按 implementation lane 完成 IMP-75：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认在 describe 已限定 legacy route 上下文下两条 route 级 API 失败场景继续保持对称 `route` 前缀会造成多余冗词，因此仅将第一条 route 级失败标题从 `route getIssue rejects` 收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 193 | 2026-04-20 | 按 implementation lane 完成 IMP-74：复核第二条 route 级 API 失败场景 `getOrganizations rejects` 是否应与已补回的 `route getIssue rejects` 保持最小对称 route 语义；确认若继续保留无前缀标题会削弱与 helper 级标题的层级区分，因此仅将其补回为 `route getOrganizations rejects`，保持第一条标题、共享断言/fixture 与 helper/route 运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 192 | 2026-04-20 | 按 implementation lane 完成 IMP-73：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认 route 级两条 API 失败场景若都维持 `getIssue rejects` / `getOrganizations rejects` 会与 helper 层级过近，因此仅将第一条 route 级失败标题补回为 `route getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 191 | 2026-04-20 | 按 implementation lane 完成 IMP-72：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认在 describe 已限定 legacy route 上下文下第二条 route 级 API 失败场景 `route getOrganizations rejects` 应与已收口为 `getIssue rejects` 的第一条标题保持对称，也去掉 `route` 前缀，因此仅将第二条标题收口为 `getOrganizations rejects`，保持第一条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
| Linear 对标 | Session 190 | 2026-04-20 | 按 implementation lane 完成 IMP-71：复核 helper 级 `returns the empty props contract when the helper workspace slug lookup misses` 仍保留 helper seam 语义，确认在 describe 已限定 legacy route 上下文下两条 route 级 API 失败场景继续保持对称 `route` 前缀会造成多余冗词，因此仅将第一条 route 级失败标题从 `route getIssue rejects` 收口为 `getIssue rejects`，保持第二条标题、共享断言/fixture 与运行时逻辑不变；完成定向验证、feature commit 与状态回写 | 0.2h | 0.2h |
